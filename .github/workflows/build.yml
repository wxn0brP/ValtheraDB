name: Build Nightly and Release

on:
  push:
    branches:
      - main
    tags:
      - '*'

  workflow_dispatch:

jobs:
  build:
    uses: wxn0brP/workflow-dist/.github/workflows/build-ts.yml@main
    with:
      publishToNpm: true