{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"ValtheraDB: Your Data, Your Rules","text":"<p>Welcome to ValtheraDB, a modular, embedded database for developers who want to build their perfect data layer. With a familiar API and unparalleled flexibility, ValtheraDB empowers you to take control of your data storage!</p>"},{"location":"#our-philosophy-control-and-flexibility","title":"Our Philosophy: Control and Flexibility","text":"<p>In a world of one-size-fits-all solutions, ValtheraDB is different. We believe that you, the developer, should have the final say on how your data is managed. Our core philosophy is built on two pillars:</p> <ul> <li>Unmatched Modularity: The storage engine is just a plugin. Don't like JSON files? Use a single binary file, YAML, <code>localStorage</code>, or invent your own format. ValtheraDB's architecture is designed to adapt to your needs, not the other way around.</li> <li>Pragmatic Power: We provide powerful features like cross-database relations and a rich query API, but we keep it simple. ValtheraDB is designed for small to medium-sized applications where a custom-fit and developer experience are more important than supporting massive datasets.</li> </ul>"},{"location":"#who-is-valtheradb-for","title":"Who is ValtheraDB for?","text":"<p>ValtheraDB is a great fit if you are:</p> <ul> <li>A Node.js or Bun developer building a backend and wanting an easy-to-use, embedded database without the overhead of a separate database server.</li> <li>A frontend developer creating a Progressive Web App (PWA) that needs offline capabilities or complex client-side storage.</li> <li>An Electron developer who needs a straightforward way to store data locally in a desktop application.</li> <li>A creative coder who wants to experiment with unconventional storage methods for your projects.</li> <li>A developer frustrated with ORMs who wants the data access pattern of an object database without learning SQL syntax.</li> <li>A team building an MVP or Electron app that will eventually outgrow in-memory arrays but doesn't need 10TB sharded infrastructure.</li> </ul> <p>In short, if you value flexibility and control over rigid conventions, you'll feel right at home.</p>"},{"location":"#key-features-at-a-glance","title":"Key Features at a Glance","text":"<ul> <li>Pluggable Storage Engine: Bring your own storage adapter.</li> <li>Powerful Cross-Database Relations: Create relationships between data across entirely separate database instances.</li> <li>Familiar MongoDB-like API: Start working quickly with an intuitive and expressive query language.</li> <li>Runs Everywhere: Optimized for Bun, great with Node.js, and fully capable in the browser.</li> <li>Client-Server Ready: Scale from an embedded solution to a client-server architecture when you need to.</li> <li>Zero Configuration: Point it to a directory, and you're good to go.</li> <li>Flexible API: <code>ValtheraCreate()</code> for simplicity, <code>VDB()</code> for adapter switching via environment variables.</li> <li>Multiple Backends: Dir, SQLite, MongoDB, browser storage, and more.</li> </ul>"},{"location":"#where-to-go-next","title":"Where to Go Next?","text":"<ul> <li>Getting Started: Jump into our hands-on tutorial and build your first application with ValtheraDB.</li> <li>Core Concepts: Learn about the fundamental ideas that make ValtheraDB unique.</li> <li>Versioning: Understand how ValtheraDB handles versioning and compatibility.</li> <li>API Reference:<ul> <li>Search Operators</li> <li>Find Options</li> <li>DB Find Options</li> <li>Collection</li> <li>Valthera Class</li> <li>Memory DB</li> <li>Multi Storage</li> <li>Forge</li> <li>ID Generation</li> <li>Relations</li> <li>Update Operators</li> <li>Remote</li> </ul> </li> <li>Ecosystem:<ul> <li>Storage Adapters: All supported storage backends with quick-start guides.</li> <li>Server: Deploy your own ValtheraDB HTTP server.</li> <li>CLI: Command-line tool for database management.</li> <li>Conduit: Embed ValtheraDB from any language via stdio protocol.</li> <li>Extensions: CRDT replication, indexing, and file locking.</li> <li>Other Tools: Benchmark, E2E tests, snapshot, resolver, and more.</li> </ul> </li> <li>Dev's Tutorials:<ul> <li>Adapters: Step-by-step guide to creating your own storage adapter.</li> <li>Plugins: Learn how to write plugins.</li> <li>Executor: Understand how operations are queued and executed.</li> </ul> </li> </ul>"},{"location":"core_concepts/","title":"ValtheraDB Idea: A Database That Thinks Like a Developer","text":"<p>ValtheraDB is a project born from a simple conviction: a database should adapt to your programming style, not the other way around.</p> <p>Instead of yet another implementation of rigid paradigms, we created a flexible environment that understands the real challenges of modern applications. It is not just a tool for storing data, it is a philosophy of software development.</p>"},{"location":"core_concepts/#pluggable-storage-paradigm-your-vision-your-medium","title":"Pluggable Storage Paradigm: Your Vision, Your Medium","text":""},{"location":"core_concepts/#the-ideas-behind-the-concept","title":"The ideas behind the concept","text":"<p>In traditional databases, form determines substance, the choice of SQL vs NoSQL, local files vs server, defines the architecture of the entire application. ValtheraDB reverses this logic.</p> <p>The central idea: separating data logic from physical storage. Your code operates on objects and relationships, while the method of persistence is a replaceable module.</p>"},{"location":"core_concepts/#why-is-this-important","title":"Why is this important?","text":"<ol> <li>Evolution without revolution    Start with JSON files during prototyping. Move to IndexedDB for PWAs. End up with a remote server in production. All with the same business logic.</li> <li>A natural language for data    You do not think in \"tables\", \"documents\", or \"graphs\". You think in \"users\", \"orders\", \"events\". ValtheraDB speaks your language.</li> <li>Architecture without compromise    Every application has unique requirements. ValtheraDB lets you choose the optimal solution without sacrificing developer convenience.</li> </ol>"},{"location":"core_concepts/#metaphor-file-system-vs-cloud","title":"Metaphor: File system vs cloud","text":"<p>Just as a file browser works the same for local disks and Dropbox, ValtheraDB provides a unified API regardless of backend. It is abstraction that truly abstracts.</p>"},{"location":"core_concepts/#relation-engine-unity-in-diversity","title":"Relation Engine: Unity in Diversity","text":""},{"location":"core_concepts/#the-problem-we-are-solving","title":"The problem we are solving","text":"<p>Modern applications are ecosystems: microservices, modules, separate databases for different functions. Traditional databases force a choice: either a monolith (everything in one DB) or chaos (manually gluing distributed data).</p> <p>ValtheraDB proposes a third path: autonomous, yet connected data collections.</p>"},{"location":"core_concepts/#the-philosophy-of-relationships","title":"The philosophy of relationships","text":"<ol> <li>Declarativity over imperativity    Instead of writing algorithms for data joins, you describe relationships between them. The system executes your intent.</li> <li>Context preserved    Data remains in its natural environment (logs in a separate store, users in the main one), but forms a coherent whole during queries.</li> <li>Semantics over syntax    It does not matter how the data is stored - what matters is what it means and how it connects.</li> </ol>"},{"location":"core_concepts/#example-in-action","title":"Example in action","text":"<p>Imagine an e-commerce application:</p> <ul> <li>Main database: products, users</li> <li>Separate database: reviews (frequent updates)</li> <li>Another database: analytics logs</li> </ul> <p>In the traditional approach - three different queries, manual mapping. In ValtheraDB - one query combines everything as if it lived in one place.</p>"},{"location":"core_concepts/#principle-of-symmetry-the-same-code-every-platform","title":"Principle of Symmetry: The Same Code, Every Platform","text":""},{"location":"core_concepts/#unified-experience","title":"Unified experience","text":"<p>ValtheraDB stems from a simple observation: developers write business logic, not storage implementations. Therefore, we provide:</p> <ol> <li>Core everywhere, adapters per environment    The Valthera core runs identically in Node.js, the browser, and Electron - no environment-specific code. Adapters mediate storage and are naturally tied to their environments, but your business logic remains portable.</li> <li>Progressive specialization    Start with the simplest configuration (JSON files). Move to the advanced one (remote server) without changing a line of application code.</li> <li>Expressiveness without complexity    Powerful capabilities through a simple API. Complex inside, simple outside.</li> </ol>"},{"location":"core_concepts/#security-by-design","title":"Security by Design","text":"<p>ValtheraDB treats root-level operators as an internal feature. Users cannot accidentally turn their data input into query operators, which prevents NoSQL injection attacks by design. This is a deliberate choice, not a limitation. You can build with confidence knowing that malicious input cannot manipulate your queries.</p>"},{"location":"core_concepts/#valtheradb-in-the-developer-ecosystem","title":"ValtheraDB in the Developer Ecosystem","text":""},{"location":"core_concepts/#who-is-this-database-for","title":"Who is this database for?","text":"<p>ValtheraDB works well for several types of developers:</p> <ol> <li>PWA creators who want consistent online and offline experience without wrestling with the IndexedDB API.</li> <li>Microservices architects who need flexible data linking across multiple sources.</li> <li>Experimenters and prototypes who value speed of iteration over premature optimization.</li> <li>Electron app developers who need local storage that feels natural in JavaScript.</li> <li>Developers frustrated with SQL and ORMs who want a MongoDB-like API without running a database server.</li> </ol>"},{"location":"core_concepts/#philosophy-in-practice","title":"Philosophy in practice","text":"<p>ValtheraDB is not trying to win any benchmark wars. It exists for developers who need something more powerful than juggling arrays in memory, but do not want to deal with the operational overhead of running a database cluster.</p> <p>Think of it as a flexible layer: you can start with JSON files, plug in SQLite when you need more structure, or even sit ValtheraDB in front of PostgreSQL to get its API while keeping your existing infrastructure. The goal is developer convenience, not enterprise credibility.</p>"},{"location":"core_concepts/#where-it-fits","title":"Where it fits","text":"<p>ValtheraDB works well when you are building an Electron app, a progressive web app, a simple server, or an MVP that will eventually need more. It is also useful as a unifying layer when you have multiple data sources and want one consistent interface to work with them.</p> <p>It is not the right choice if you need petabyte-scale storage, complex distributed transactions, or dedicated DBA teams. For everything else, it tries to stay out of your way.</p>"},{"location":"core_concepts/#summary","title":"Summary","text":"<p>ValtheraDB is a flexible data layer for developers who want to control how their data is stored without being locked into enterprise solutions.</p> <p>It adapts to your needs: start simple, scale when necessary, and switch backends without rewriting your application logic. No benchmarks to win, no clusters to manage. Just a tool that works the way you expect.</p> <p>Ready to try it? Begin your journey.</p>"},{"location":"getting_started/","title":"Getting Started: Building a Mini-Blog","text":"<p>Welcome to ValtheraDB! This guide will walk you through the entire process of building a simple application-a mini-blog-to demonstrate how to get up and running quickly. We'll cover everything from installation to using powerful features like relations.</p> <p>By the end of this tutorial, you will have learned how to:</p> <ul> <li>Install ValtheraDB</li> <li>Create a database and collections</li> <li>Add and query for data</li> <li>Link related data together</li> </ul>"},{"location":"getting_started/#step-1-installation","title":"Step 1: Installation","text":"<p>First things first, let's add ValtheraDB to your project. Open your terminal and run the following command:</p> <ul> <li> <p>Using npm   <pre><code>npm install @wxn0brp/db\n</code></pre></p> </li> <li> <p>Or using bun   <pre><code>bun add @wxn0brp/db\n</code></pre></p> </li> <li> <p>Or using yarn   <pre><code>yarn add @wxn0brp/db\n</code></pre></p> </li> </ul> <p>Now you're ready to start using it in your code.</p>"},{"location":"getting_started/#step-2-setting-up-the-database","title":"Step 2: Setting Up the Database","text":"<p>ValtheraDB is an embedded database, which means it runs directly within your application. To get started, you just need to point it to a directory where it can store its data.</p>"},{"location":"getting_started/#simple-setup-dir-adapter","title":"Simple Setup (dir adapter)","text":"<p>Let's create our database instance using <code>ValtheraCreate()</code>:</p> <pre><code>import { ValtheraCreate } from \"@wxn0brp/db\";\n\n// Create a new database instance inside the \"./my-blog-db\" directory\nconst db = ValtheraCreate&lt;{\n  // Define your collections here (for type checking)\n  users: {\n    name: string;\n    email: string;\n    _id: string;\n  };\n  posts: {\n    title: string;\n    content: string;\n    authorId: string;\n    _id: string;\n  };\n}&gt;(\"./my-blog-db\");\n</code></pre>"},{"location":"getting_started/#flexible-setup-switch-adapters-via-env","title":"Flexible Setup (switch adapters via env)","text":"<p>If you may need to switch storage adapters later, use <code>VDB()</code>:</p> <pre><code>import { VDB } from \"@wxn0brp/db\";\n\n// Create a new database instance - same API, but adapter can be changed via env\nconst db = VDB&lt;{\n  users: {\n    name: string;\n    email: string;\n    _id: string;\n  };\n  posts: {\n    title: string;\n    content: string;\n    authorId: string;\n    _id: string;\n  };\n}&gt;().dir(\"./my-blog-db\");\n\n// Later, switch to SQLite without code changes:\n// VALTHERA_MASTER=sqlite VALTHERA_MASTER_OPTS=[\"./db.sqlite\"] bun run app.ts\n</code></pre> <p>When to use which?</p> <ul> <li><code>ValtheraCreate()</code> - simple apps, prototypes, dir adapter only</li> <li><code>VDB()</code> - need to switch adapters, env-driven deployment, production apps</li> </ul> <p>If you run this code, a new folder named <code>my-blog-db</code> will be created in your project. This is where all your collections and documents will live. It's that simple!</p>"},{"location":"getting_started/#step-3-adding-your-first-user","title":"Step 3: Adding Your First User","text":"<p>Our blog will have authors, so let's create a <code>users</code> collection and add our first author to it. A \"collection\" is just a group of similar documents, like a table in a SQL database.</p> <p>The <code>db.add()</code> method adds a new document to a specified collection and automatically generates a unique <code>_id</code> for it.</p> <pre><code>async function setup() {\n  // Add a new document to the \"users\" collection\n  const author = await db.users.add({\n    name: \"Jane Doe\",\n    email: \"jane.doe@example.com\"\n  });\n\n  console.log(\"Successfully added author:\", author);\n  // Expected output:\n  // Successfully added author: { name: 'Jane Doe', email: 'jane.doe@example.com', _id: '...' }\n}\n\nsetup();\n</code></pre>"},{"location":"getting_started/#step-4-adding-posts-for-our-user","title":"Step 4: Adding Posts for Our User","text":"<p>Now that we have an author, let's give them something to write. We'll create a <code>posts</code> collection. To link a post to its author, we'll add an <code>authorId</code> field to our post documents, storing the <code>_id</code> of the user we just created.</p> <pre><code>async function setup() {\n  // First, find our author to get their ID\n  const author = await db.users.findOne({ email: \"jane.doe@example.com\" });\n\n  if (!author) {\n    console.log(\"Author not found!\");\n    return;\n  }\n\n  // Now, create the \"posts\" collection\n  const posts = db.posts;\n\n  // Next, add a couple of posts linked to the author\n  const post1 = await posts.add({\n    title: \"My First Post\",\n    content: \"This is the content of my first post.\",\n    authorId: author._id // Link to the author\n  });\n\n  const post2 = await posts.add({\n    title: \"Advanced Concepts\",\n    content: \"Exploring the depths of ValtheraDB.\",\n    authorId: author._id // Link to the same author\n  });\n\n  console.log(\"Added posts:\", post1, post2);\n}\n\nsetup();\n</code></pre> <p>Here we used <code>db.findOne()</code> to retrieve a single document that matches our query. It's a quick way to find a specific entry.</p>"},{"location":"getting_started/#step-5-updating-data","title":"Step 5: Updating Data","text":"<p>Sometimes, data changes! ValtheraDB provides easy ways to update existing documents. Let's say Jane Doe decided to update her email address. We can use <code>db.updateOne()</code> to modify her user record.</p> <pre><code>async function updateAuthor() {\n  const users = db.users;\n  const updated = await users.updateOne(\n    { name: \"Jane Doe\" }, // Find Jane Doe\n    { email: \"jane.d.new@example.com\" } // Update her email\n  );\n\n  if (updated) {\n    console.log(\"Author's email updated successfully!\");\n  } else {\n    console.log(\"Author not found or no changes made.\");\n  }\n\n  // Verify the update\n  const jane = await users.findOne({ name: \"Jane Doe\" });\n  console.log(\"Updated Jane Doe:\", jane);\n}\n\nupdateAuthor();\n</code></pre> <p>The <code>db.updateOne()</code> method takes a search query and an updater object. It finds the first document matching the query and applies the updates. For more complex updates (like incrementing numbers or manipulating arrays), you can use Updater Operators.</p>"},{"location":"getting_started/#step-6-updating-or-adding-upsert","title":"Step 6: Updating or Adding (Upsert)","text":"<p>Sometimes you need to ensure that a piece of data exists: updating it if it's already there or creating it if it's new. This is a common pattern known as \"upsert\", and ValtheraDB supports it with <code>db.updateOneOrAdd()</code>.</p> <p>Let's try to update a post. If the post with a specific title exists, we'll update its content. If not, we'll add it as a new post.</p> <pre><code>async function upsertPost() {\n  // First, get the author's ID for linking new posts\n  const author = await db.users.findOne({ email: \"jane.d.new@example.com\" });\n  if (!author) {\n    console.error(\"Author not found for upsert operation.\");\n    return;\n  }\n\n  const posts = db.posts;\n\n  // Scenario 1: Update an existing post\n  const updatedExisting = await posts.updateOneOrAdd(\n    { title: \"My First Post\" }, // Search for this post\n    { content: \"This is the *updated* content of my first post.\" } // Update its content\n  );\n  console.log(\"Updated existing post?\", updatedExisting);\n\n  // Scenario 2: Add a new post if it doesn't exist\n  const addedNew = await posts.updateOneOrAdd(\n    { title: \"A Brand New Post\" }, // Search for this post (it won't exist yet)\n    {\n      content: \"This post was created using updateOneOrAdd.\",\n    }, // Data to update/add\n    { \n      add_arg: {\n        authorId: author._id\n      } // Data to add if new\n    }\n  );\n  console.log(\"Added new post?\", addedNew);\n\n  // Verify the changes\n  const userPosts = await posts.find({ authorId: author._id });\n  console.log(\"All posts after upsert:\", userPosts);\n}\n\nupsertPost();\n</code></pre> <p><code>db.updateOneOrAdd()</code> is incredibly flexible. The <code>add_arg</code> option allows you to specify additional data to be merged when a new document is created.</p>"},{"location":"getting_started/#step-7-bringing-it-all-together-with-relations","title":"Step 7: Bringing It All Together with Relations","text":"<p>We now have users and posts, but they are in separate collections. How do we retrieve a user and all their posts in a single query? This is where ValtheraDB's <code>Relation</code> engine shines.</p> <p>Relations allow you to define relationships between your collections and fetch linked data effortlessly.</p> <p>Let's find our author and embed all of their posts directly into the result.</p> <pre><code>async function findUserWithPosts() {\n  // 1. The Relation class needs an object mapping names to database instances.\n  const dbs = {\n    main: db,\n  };\n\n  // 2. Define the relationship.\n  // We're creating a relation named \"posts\".\n  const relations: RelationTypes.Relation = {\n    posts: {\n      // It finds data in the \"posts\" collection of the \"main\" db.\n      path: [\"main\", \"posts\"],\n      // It's a \"one-to-many\" relationship.\n      type: \"1n\",\n      // It matches documents where the \"authorId\" in the \"posts\" collection...\n      fk: \"authorId\",\n      // ...is equal to the \"_id\" in the \"users\" collection.\n      pk: \"_id\"\n    },\n  };\n\n  // 3. Create a new Relation instance.\n  const relation = new Relation(dbs);\n\n  // 4. Find the user and apply the relations.\n  const authorWithPosts = await relation.findOne(\n    [\"main\", \"users\"], // The collection we are querying\n    { name: \"Jane Doe\" }, // The user we are looking for\n    relations // The relations to apply\n  );\n\n  console.dir(authorWithPosts, { depth: null });\n}\n\nfindUserWithPosts();\n</code></pre> <p>The <code>console.dir</code> output will show you the user object with a new <code>posts</code> array field containing the full documents of both posts we created.</p> <pre><code>{\n  \"name\": \"Jane Doe\",\n  \"email\": \"jane.d.new@example.com\",\n  \"_id\": \"...\",\n  \"posts\": [\n    {\n      \"title\": \"My First Post\",\n      \"content\": \"This is the content of my first post.\",\n      \"authorId\": \"...\",\n      \"_id\": \"...\"\n    },\n    {\n      \"title\": \"Advanced Concepts\",\n      \"content\": \"Exploring the depths of ValtheraDB.\",\n      \"authorId\": \"...\",\n      \"_id\": \"...\"\n    }\n    // ...\n  ]\n}\n</code></pre>"},{"location":"getting_started/#whats-next","title":"What's Next?","text":"<p>Congratulations! You've just scratched the surface of what ValtheraDB can do.</p> <ul> <li>Ready to learn about the fundamental ideas behind ValtheraDB? Dive into our Core Concepts page.</li> <li>Want to see all the powerful ways you can query your data? Check out the Search Options.</li> <li>Curious about how to perform complex data updates? Read the Updater documentation.</li> </ul>"},{"location":"versioning/","title":"Ecosystem Versioning &amp; Compatibility Policy","text":"<p>This document explains how versioning works across our multi-repo structure, specifically focusing on the relationship between the core library, the meta-package, and storage adapters.</p>"},{"location":"versioning/#1-package-structure","title":"1. Package Structure","text":"<p>Our ecosystem consists of three types of packages:</p> <ol> <li><code>@wxn0brp/db-core</code> \u2013 The foundational logic and interfaces.</li> <li><code>@wxn0brp/db</code> \u2013 The meta-package (entry point) that re-exports core functionality.</li> <li>Adapters (e.g., <code>@wxn0brp/db-storage-dir</code>) \u2013 Implementation-specific packages that depend on <code>db-core</code>.</li> </ol>"},{"location":"versioning/#2-core-meta-package-versioning","title":"2. Core &amp; Meta Package Versioning","text":"<p>The relationship between the meta-package and the core is synchronized.</p> Package Version Meaning <code>@wxn0brp/db-core</code> <code>0.11.0</code> Core logic version <code>@wxn0brp/db</code> <code>0.110.0</code> Matches Core version <ul> <li>Rule: <code>@wxn0brp/db</code> always mirrors the <code>Minor</code> and <code>Patch</code> version of <code>@wxn0brp/db-core</code>.</li> <li>Exception: If the meta-package itself requires a fix independent of core, the <code>Patch</code> version may increment slightly higher, but the <code>Minor</code> version will always match the Core.</li> </ul>"},{"location":"versioning/#3-adapter-versioning-scheme-0cab","title":"3. Adapter Versioning Scheme (<code>0.CA.B</code>)","text":"<p>Adapters have independent lifecycles but strict compatibility requirements with <code>db-core</code>. To make compatibility visible directly in the version number, we use a custom encoding scheme.</p>"},{"location":"versioning/#the-formula","title":"The Formula","text":"<pre><code>0.CA.B\n\u2502 || |-- B: Adapter Patch (Bugfixes or Core Patch requirement)\n\u2502 ||---- A: Adapter Series (Adapter Breaking Changes)\n\u2502 |----- C: Core Minor Version\n|------- Major (Always 0)\n</code></pre>"},{"location":"versioning/#examples","title":"Examples","text":"Adapter Version Decoded Meaning <code>0.110.5</code> Works with Core 0.11.x, Adapter Series 0, Patch 5 <code>0.111.0</code> Works with Core 0.11.x, Adapter Series 1 (Breaking Change), Patch 0 <code>0.120.0</code> Works with Core 0.12.x, Adapter Series 0, Patch 0 <code>0.110.6</code> Works with Core 0.11.x, Updated to require Core 0.11.2 (Patch requirement)"},{"location":"versioning/#version-change-triggers","title":"Version Change Triggers","text":"Event Adapter Version Change Example Adapter Bugfix <code>B + 1</code> <code>0.110.5</code> -&gt; <code>0.110.6</code> Adapter Breaking Change <code>A + 1</code>, <code>B = 0</code> <code>0.110.5</code> -&gt; <code>0.111.0</code> Core Minor Update (e.g., 0.11 -&gt; 0.12) <code>C + 1</code>, <code>A = 0</code>, <code>B = 0</code> <code>0.111.5</code> -&gt; <code>0.120.0</code> Core Patch Requirement (e.g., security fix) <code>B + 1</code> <code>0.110.5</code> -&gt; <code>0.110.6</code>"},{"location":"api/collection/","title":"Collection Class Documentation","text":"<p>This documentation provides a detailed overview of the <code>Collection</code> class, designed for performing CRUD operations on Valthera collections.</p>"},{"location":"api/collection/#class-collectiont-extends-object-data","title":"Class: <code>Collection&lt;T extends object = Data&gt;</code>","text":""},{"location":"api/collection/#usage","title":"Usage:","text":"<pre><code>const db = ValtheraCreate(\"db\");\nconst users = db.c(\"users\");\n// or with forgeTypedValthera:\nconst users = db.users;\n</code></pre>"},{"location":"api/collection/#async-adddata-id_gentrue","title":"<code>async add(data, id_gen=true)</code>","text":"<p>Adds data to the collection.</p> <ul> <li> <p>Parameters:</p> <ul> <li><code>data</code> (<code>Arg&lt;D&gt;</code>): The data to add.</li> <li><code>id_gen</code> (<code>boolean</code>, default: true): Whether to generate an ID for the entry.</li> </ul> </li> <li> <p>Returns:</p> <ul> <li><code>Promise&lt;T &amp; { _id: string }&gt;</code>: If <code>id_gen</code> is true, a promise that resolves with the added data including a generated <code>_id</code>.</li> <li><code>Promise&lt;T&gt;</code>: If <code>id_gen</code> is false, a promise that resolves with the added data.</li> </ul> </li> </ul>"},{"location":"api/collection/#async-findsearch-options-findopts-context","title":"<code>async find(search, options, findOpts, context)</code>","text":"<p>Finds data in the collection based on a query.</p> <ul> <li> <p>Parameters:</p> <ul> <li><code>search</code> (<code>Search&lt;D&gt;</code>, optional, default: <code>{}</code>): The search query.</li> <li><code>options</code> (<code>DbFindOpts&lt;D&gt;</code>, optional, default: <code>{}</code>): Post-retrieval options (e.g., <code>max</code>, <code>reverse</code>, <code>sortBy</code>, <code>limit</code>, <code>offset</code>).</li> <li><code>findOpts</code> (<code>FindOpts&lt;D&gt;</code>, optional, default: <code>{}</code>): Post-matching options (<code>select</code>, <code>exclude</code>, <code>transform</code>).</li> <li><code>context</code> (<code>VContext</code>, optional, default: <code>{}</code>): The context object (for function-based queries).</li> </ul> </li> <li> <p>Returns:</p> <ul> <li><code>Promise&lt;D[]&gt;</code>: Found data. Empty array if no match is found.</li> </ul> </li> </ul>"},{"location":"api/collection/#async-findonesearch-findopts-context","title":"<code>async findOne(search, findOpts, context)</code>","text":"<p>Finds one matching entry in the collection.</p> <ul> <li> <p>Parameters:</p> <ul> <li><code>search</code> (<code>Search&lt;D&gt;</code>, optional, default: <code>{}</code>): The search query.</li> <li><code>findOpts</code> (<code>FindOpts&lt;D&gt;</code>, optional, default: <code>{}</code>): Post-matching options.</li> <li><code>context</code> (<code>VContext</code>, optional, default: <code>{}</code>): The context object (for function-based queries).</li> </ul> </li> <li> <p>Returns:</p> <ul> <li><code>Promise&lt;D&gt;</code>: Found data. <code>undefined</code> if no match is found.</li> </ul> </li> </ul>"},{"location":"api/collection/#async-updatesearch-updater-context","title":"<code>async update(search, updater, context)</code>","text":"<p>Updates data in the collection.</p> <ul> <li> <p>Parameters:</p> <ul> <li><code>search</code> (<code>Search&lt;D&gt;</code>): The search query.</li> <li><code>updater</code> (<code>Updater&lt;D&gt;</code>): Update arguments.</li> <li><code>context</code> (<code>VContext</code>, optional, default: <code>{}</code>): The context object (for function-based queries).</li> </ul> </li> <li> <p>Returns:</p> <ul> <li><code>Promise&lt;D[]&gt;</code>: Updated data. Empty array if no match is found.</li> </ul> </li> </ul>"},{"location":"api/collection/#async-updateonesearch-updater-context","title":"<code>async updateOne(search, updater, context)</code>","text":"<p>Updates one entry in the collection.</p> <ul> <li> <p>Parameters:</p> <ul> <li><code>search</code> (<code>Search&lt;D&gt;</code>): The search query.</li> <li><code>updater</code> (<code>Updater&lt;D&gt;</code>): Update arguments.</li> <li><code>context</code> (<code>VContext</code>, optional, default: <code>{}</code>): The context object (for function-based queries).</li> </ul> </li> <li> <p>Returns:</p> <ul> <li><code>Promise&lt;D | null&gt;</code>: Updated data. Null if no match is found.</li> </ul> </li> </ul>"},{"location":"api/collection/#async-removesearch-context","title":"<code>async remove(search, context)</code>","text":"<p>Removes data from the collection.</p> <ul> <li> <p>Parameters:</p> <ul> <li><code>search</code> (<code>Search&lt;D&gt;</code>): The search query.</li> <li><code>context</code> (<code>VContext</code>, optional, default: <code>{}</code>): The context object (for function-based queries).</li> </ul> </li> <li> <p>Returns:</p> <ul> <li><code>Promise&lt;D[]&gt;</code>: Removed data. Empty array if no match is found.</li> </ul> </li> </ul>"},{"location":"api/collection/#async-removeonesearch-context","title":"<code>async removeOne(search, context)</code>","text":"<p>Removes one entry from the collection.</p> <ul> <li> <p>Parameters:</p> <ul> <li><code>search</code> (<code>Search&lt;D&gt;</code>): The search query.</li> <li><code>context</code> (<code>VContext</code>, optional, default: <code>{}</code>): The context object (for function-based queries).</li> </ul> </li> <li> <p>Returns:</p> <ul> <li><code>Promise&lt;D | null&gt;</code>: Removed data. Null if no match is found.</li> </ul> </li> </ul>"},{"location":"api/collection/#async-updateoneoraddsearch-updater-options","title":"<code>async updateOneOrAdd(search, updater, options)</code>","text":"<p>Updates one entry or adds a new one if it doesn't exist.</p> <ul> <li> <p>Parameters:</p> <ul> <li><code>search</code> (<code>Search&lt;D&gt;</code>): The search query.</li> <li><code>updater</code> (<code>Updater&lt;D&gt;</code>): Update arguments.</li> <li><code>options</code> (<code>Object</code>, optional): An object containing:<ul> <li><code>add_arg</code> (<code>Arg&lt;D&gt;</code>, default: <code>{}</code>): Data to merge when adding a new entry.</li> <li><code>context</code> (<code>VContext</code>, default: <code>{}</code>): The context object (for function-based queries).</li> <li><code>id_gen</code> (<code>boolean</code>, default: true): Whether to generate an ID for the new entry.</li> </ul> </li> </ul> </li> <li> <p>Returns:</p> <ul> <li><code>Promise&lt;{ data: T; type: \"added\" | \"updated\" }&gt;</code>: A promise that resolves with the updated or added entry.</li> </ul> </li> </ul>"},{"location":"api/collection/#async-toggleonesearch-data-context","title":"<code>async toggleOne(search, data, context)</code>","text":"<p>Removes one entry if it exists, or adds a new one if it doesn't. Usage e.g. for toggling a flag.</p> <ul> <li> <p>Parameters:</p> <ul> <li><code>search</code> (<code>Search&lt;D&gt;</code>): The search query.</li> <li><code>data</code> (<code>Arg&lt;D&gt;</code>, optional, default: <code>{}</code>): The data to use when adding.</li> <li><code>context</code> (<code>VContext</code>, optional, default: <code>{}</code>): The context object (for function-based queries).</li> </ul> </li> <li> <p>Returns:</p> <ul> <li><code>Promise&lt;{ data: T; type: \"added\" | \"removed\" }&gt;</code>: A promise that resolves with the removed or added entry.</li> </ul> </li> </ul>"},{"location":"api/db_find_opts/","title":"DbFindOpts (Post-Retrieval Processing)","text":""},{"location":"api/db_find_opts/#description","title":"Description","text":"<p>The <code>dbFindOpts</code> parameter provides options for post-processing data after it has been retrieved from the database but before it is returned to the client. These options control:</p> <ol> <li>Ordering - Sort results by a field</li> <li>Pagination - Skip and limit the number of returned entries</li> <li>Direction - Reverse the order of results</li> <li>Aggregation - Group, count, min, max, and average over fields</li> </ol> <p>This allows for efficient data manipulation without requiring additional processing on the client side.</p>"},{"location":"api/db_find_opts/#options","title":"Options","text":""},{"location":"api/db_find_opts/#reverse","title":"<code>reverse</code>","text":"<p>Reverses the order of the results.</p> <pre><code>// Query\n{\n    dbFindOpts: {\n        reverse: true\n    }\n}\n\n// If original results are: [1, 2, 3, 4, 5]\n// Final results will be: [5, 4, 3, 2, 1]\n</code></pre> <p>Note: When <code>sortBy</code> is not specified, <code>reverse</code> is applied immediately during file iteration. When <code>sortBy</code> is used, the final sort order is determined by <code>sortAsc</code>.</p>"},{"location":"api/db_find_opts/#offset","title":"<code>offset</code>","text":"<p>Skips a specified number of entries from the beginning of the results.</p> <pre><code>// Query\n{\n    dbFindOpts: {\n        offset: 10\n    }\n}\n\n// Skips the first 10 entries and returns the rest\n</code></pre> <p>Default: <code>0</code></p>"},{"location":"api/db_find_opts/#limit","title":"<code>limit</code>","text":"<p>Limits the number of returned entries.</p> <pre><code>// Query\n{\n    dbFindOpts: {\n        limit: 20\n    }\n}\n\n// Returns only the first 20 entries (after offset is applied)\n</code></pre> <p>Default: <code>-1</code> (no limit)</p>"},{"location":"api/db_find_opts/#sortby","title":"<code>sortBy</code>","text":"<p>Sorts results by a specified field name, randomly, or by multiple fields.</p> <pre><code>// Sort by a field\n{\n    dbFindOpts: {\n        sortBy: \"age\"\n    }\n}\n\n// Random sort\n{\n    dbFindOpts: {\n        sortBy: \"random()\"\n    }\n}\n\n// Multi-field sort\n{\n    dbFindOpts: {\n        sortBy: [\n            { field: \"lastName\", asc: true },\n            { field: \"age\", asc: false }\n        ]\n    }\n}\n</code></pre> <p>Special value: <code>\"random()\"</code> - Shuffles results randomly using <code>Math.random()</code>.</p> <p>Multi-field sort: Pass an array of <code>{ field, asc? }</code> objects. Each field is sorted in order; subsequent fields break ties from previous comparisons. <code>asc</code> defaults to <code>true</code>.</p> <p>Default: <code>undefined</code> (no sorting, uses data order)</p>"},{"location":"api/db_find_opts/#sortasc","title":"<code>sortAsc</code>","text":"<p>Determines the sort direction when <code>sortBy</code> is specified.</p> <pre><code>// Ascending order (default)\n{\n    dbFindOpts: {\n        sortBy: \"age\",\n        sortAsc: true\n    }\n}\n\n// Descending order\n{\n    dbFindOpts: {\n        sortBy: \"age\",\n        sortAsc: false\n    }\n}\n</code></pre> <p>Default: <code>true</code></p>"},{"location":"api/db_find_opts/#groupby","title":"<code>groupBy</code>","text":"<p>Groups results by one or more fields. When specified alongside aggregation operators (<code>min</code>, <code>max</code>, <code>avg</code>, <code>count</code>), computations are performed per group.</p> <pre><code>// Group by a single field\n{\n    dbFindOpts: {\n        groupBy: \"category\",\n        count: { total: \"id\" }\n    }\n}\n\n// Group by multiple fields\n{\n    dbFindOpts: {\n        groupBy: [\"category\", \"status\"],\n        count: { total: \"id\" }\n    }\n}\n</code></pre> <p>Type: <code>string | string[]</code></p> <p>Default: <code>undefined</code> (no grouping, single aggregate over all data)</p>"},{"location":"api/db_find_opts/#count","title":"<code>count</code>","text":"<p>Counts non-null, non-undefined values per field. Maps output keys to source fields.</p> <pre><code>// Count total entries\n{\n    dbFindOpts: {\n        count: { total: \"id\" }\n    }\n}\n\n// Count with grouping\n{\n    dbFindOpts: {\n        groupBy: \"category\",\n        count: { total: \"id\", activeUsers: \"status\" }\n    }\n}\n</code></pre> <p>Type: <code>Record&lt;string, string&gt;</code> - <code>{ outputKey: sourceField }</code></p> <p>Default: <code>undefined</code></p>"},{"location":"api/db_find_opts/#min","title":"<code>min</code>","text":"<p>Computes the minimum numeric value of a field. Maps output keys to source fields.</p> <pre><code>// Youngest age across all entries\n{\n    dbFindOpts: {\n        min: { youngest: \"age\" }\n    }\n}\n\n// Min with grouping\n{\n    dbFindOpts: {\n        groupBy: \"category\",\n        min: { cheapest: \"price\" }\n    }\n}\n</code></pre> <p>Type: <code>Record&lt;string, string&gt;</code> - <code>{ outputKey: sourceField }</code></p> <p>Default: <code>undefined</code></p>"},{"location":"api/db_find_opts/#max","title":"<code>max</code>","text":"<p>Computes the maximum numeric value of a field. Maps output keys to source fields.</p> <pre><code>// Oldest age across all entries\n{\n    dbFindOpts: {\n        max: { oldest: \"age\" }\n    }\n}\n\n// Max with grouping\n{\n    dbFindOpts: {\n        groupBy: \"category\",\n        max: { mostExpensive: \"price\" }\n    }\n}\n</code></pre> <p>Type: <code>Record&lt;string, string&gt;</code> - <code>{ outputKey: sourceField }</code></p> <p>Default: <code>undefined</code></p>"},{"location":"api/db_find_opts/#avg","title":"<code>avg</code>","text":"<p>Computes the average (mean) numeric value of a field. Maps output keys to source fields.</p> <pre><code>// Average age across all entries\n{\n    dbFindOpts: {\n        avg: { averageAge: \"age\" }\n    }\n}\n\n// Average with grouping\n{\n    dbFindOpts: {\n        groupBy: \"category\",\n        avg: { averagePrice: \"price\" }\n    }\n}\n</code></pre> <p>Type: <code>Record&lt;string, string&gt;</code> - <code>{ outputKey: sourceField }</code></p> <p>Default: <code>undefined</code></p>"},{"location":"api/db_find_opts/#sum","title":"<code>sum</code>","text":"<p>Computes the sum of numeric values of a field. Maps output keys to source fields.</p> <pre><code>// Total revenue across all entries\n{\n    dbFindOpts: {\n        sum: { totalRevenue: \"amount\" }\n    }\n}\n\n// Sum with grouping\n{\n    dbFindOpts: {\n        groupBy: \"category\",\n        sum: { totalSales: \"price\" }\n    }\n}\n</code></pre> <p>Type: <code>Record&lt;string, string&gt;</code> - <code>{ outputKey: sourceField }</code></p> <p>Default: <code>undefined</code></p>"},{"location":"api/db_find_opts/#distinct","title":"<code>distinct</code>","text":"<p>Removes duplicate entries based on the value of a specified field. Only the first occurrence of each unique value is kept.</p> <pre><code>// Get unique categories\n{\n    dbFindOpts: {\n        distinct: \"category\"\n    }\n}\n\n// Distinct with other options\n{\n    dbFindOpts: {\n        distinct: \"email\",\n        limit: 100\n    }\n}\n</code></pre> <p>Type: <code>string</code> - The field name to deduplicate by.</p> <p>Default: <code>undefined</code> (no deduplication)</p>"},{"location":"api/db_find_opts/#execution-flow","title":"Execution Flow","text":""},{"location":"api/db_find_opts/#without-aggregation-minmaxavgsumgroupbycount","title":"Without Aggregation (<code>min</code>/<code>max</code>/<code>avg</code>/<code>sum</code>/<code>groupBy</code>/<code>count</code>)","text":"<ol> <li>Iterate through files</li> <li>Apply <code>reverse</code> during iteration (if specified)</li> <li>Apply <code>offset</code> (skip entries)</li> <li>Apply <code>limit</code> (truncate results)</li> </ol>"},{"location":"api/db_find_opts/#with-aggregation-minmaxavgsumgroupbycount","title":"With Aggregation (<code>min</code>/<code>max</code>/<code>avg</code>/<code>sum</code>/<code>groupBy</code>/<code>count</code>)","text":"<ol> <li>Collect all entries from all files</li> <li>If <code>sortBy</code> is specified, sort entries:</li> <li>If <code>sortBy === \"random()\"</code>: shuffle randomly</li> <li>If <code>sortBy</code> is an array: sort by multiple fields in order</li> <li>Otherwise: sort by field value using <code>compareSafe()</code></li> <li>Group entries by <code>groupBy</code> fields (if specified)</li> <li>Per group, compute <code>min</code>, <code>max</code>, <code>avg</code>, <code>sum</code>, <code>count</code> (if specified)</li> <li>Apply <code>distinct</code> (if specified)</li> <li>Apply <code>offset</code> and <code>limit</code> to aggregated results</li> </ol>"},{"location":"api/db_find_opts/#with-sortby-no-aggregation","title":"With <code>sortBy</code> (no aggregation)","text":"<ol> <li>Collect all entries from all files</li> <li>Sort entries:</li> <li>If <code>sortBy === \"random()\"</code>: shuffle randomly</li> <li>If <code>sortBy</code> is an array: sort by multiple fields in order</li> <li>Otherwise: sort by field value using <code>compareSafe()</code></li> <li>Apply <code>distinct</code> (if specified)</li> <li>Apply <code>offset</code> and <code>limit</code> to sorted results</li> </ol>"},{"location":"api/db_find_opts/#examples","title":"Examples","text":""},{"location":"api/db_find_opts/#basic-pagination","title":"Basic Pagination","text":"<pre><code>// Get page 2 with 10 items per page\n{\n    dbFindOpts: {\n        offset: 10,\n        limit: 10\n    }\n}\n</code></pre>"},{"location":"api/db_find_opts/#sort-by-field-descending","title":"Sort by Field Descending","text":"<pre><code>// Get users sorted by age (oldest first)\n{\n    dbFindOpts: {\n        sortBy: \"age\",\n        sortAsc: false,\n        limit: 50\n    }\n}\n</code></pre>"},{"location":"api/db_find_opts/#random-sample","title":"Random Sample","text":"<pre><code>// Get 10 random entries\n{\n    dbFindOpts: {\n        sortBy: \"random()\",\n        limit: 10\n    }\n}\n</code></pre>"},{"location":"api/db_find_opts/#reverse-order","title":"Reverse Order","text":"<pre><code>// Get results in reverse file order\n{\n    dbFindOpts: {\n        reverse: true,\n        offset: 0,\n        limit: 100\n    }\n}\n</code></pre>"},{"location":"api/db_find_opts/#combined-options","title":"Combined Options","text":"<pre><code>// Complex query with sorting and pagination\n{\n    dbFindOpts: {\n        sortBy: \"createdAt\",\n        sortAsc: false,  // Newest first\n        offset: 0,\n        limit: 20\n    }\n}\n</code></pre>"},{"location":"api/db_find_opts/#aggregate-per-group","title":"Aggregate per Group","text":"<pre><code>// Get count, min, max, avg price per category\n{\n    dbFindOpts: {\n        groupBy: \"category\",\n        count: { itemCount: \"id\" },\n        min: { minPrice: \"price\" },\n        max: { maxPrice: \"price\" },\n        avg: { avgPrice: \"price\" }\n    }\n}\n</code></pre>"},{"location":"api/db_find_opts/#global-aggregation-no-grouping","title":"Global Aggregation (no grouping)","text":"<pre><code>// Stats across all entries\n{\n    dbFindOpts: {\n        count: { total: \"id\" },\n        min: { youngest: \"age\" },\n        max: { oldest: \"age\" },\n        avg: { averageAge: \"age\" },\n        sum: { totalAge: \"age\" }\n    }\n}\n</code></pre>"},{"location":"api/db_find_opts/#multi-field-sort","title":"Multi-Field Sort","text":"<pre><code>// Sort by last name ascending, then by age descending\n{\n    dbFindOpts: {\n        sortBy: [\n            { field: \"lastName\", asc: true },\n            { field: \"age\", asc: false }\n        ]\n    }\n}\n</code></pre>"},{"location":"api/db_find_opts/#distinct-values","title":"Distinct Values","text":"<pre><code>// Get unique categories with count\n{\n    dbFindOpts: {\n        distinct: \"category\",\n        count: { total: \"id\" }\n    }\n}\n</code></pre>"},{"location":"api/find_opts/","title":"Find Options (Post-Matching Processing)","text":""},{"location":"api/find_opts/#description","title":"Description","text":"<p>These options are applied as part of a post-processing step after matching objects are found in the database (<code>find</code> stage) but before the data is returned to the client. This process is designed to: 1. Reduce data transfer size by removing unnecessary fields or selecting only the required ones. 2. Customize results by transforming objects to meet specific requirements.  </p> <p>This optimization improves both performance and result usability, ensuring the client receives precisely the needed data in a compact form.</p>"},{"location":"api/find_opts/#execution-stack","title":"Execution Stack","text":"<ol> <li><code>transform</code></li> <li><code>exclude</code></li> <li><code>select</code></li> </ol>"},{"location":"api/find_opts/#operators","title":"Operators","text":""},{"location":"api/find_opts/#select","title":"<code>select</code>","text":"<p>Selects only specific fields to include in the final object.</p> <pre><code>// Original Object\n{\n    name: \"John Doe\",\n    email: \"john@example.com\",\n    age: 30,\n    status: \"active\"\n}\n\n// Find Options\n{\n    select: [\"name\", \"email\"]\n}\n\n// Result\n{\n    name: \"John Doe\",\n    email: \"john@example.com\"\n}\n</code></pre>"},{"location":"api/find_opts/#exclude","title":"<code>exclude</code>","text":"<p>Excludes specific fields from the object, removing unwanted data.</p> <pre><code>// Original Object\n{\n    name: \"John Doe\",\n    email: \"john@example.com\",\n    age: 30,\n    status: \"active\"\n}\n\n// Find Options\n{\n    exclude: [\"name\"]\n}\n\n// Result\n{\n    email: \"john@example.com\",\n    age: 30,\n    status: \"active\"\n}\n</code></pre>"},{"location":"api/find_opts/#transform","title":"<code>transform</code>","text":"<p>Applies a custom updater function to modify the object.</p> <pre><code>// Original Object\n{\n    name: \"John Doe\",\n    email: \"john@example.com\",\n    age: 30,\n    status: \"active\"\n}\n\n// Find Options\n{\n    transform: (doc) =&gt; {\n        doc.name = doc.name.toUpperCase();\n        return doc;\n    }\n}\n\n// Result\n{\n    name: \"JOHN DOE\",\n    email: \"john@example.com\",\n    age: 30,\n    status: \"active\"\n}\n</code></pre>"},{"location":"api/find_opts/#combined-example","title":"Combined Example","text":"<p>Using all operators together to demonstrate the execution stack.</p> <pre><code>// Original Object\n{\n    name: \"John Doe\",\n    email: \"john@example.com\",\n    age: 30,\n    status: \"active\"\n}\n\n// Find Options\n{\n    transform: (doc) =&gt; {\n        doc.newField = \"added\";\n        doc.status = \"inactive\";\n        return doc;\n    },\n    exclude: [\"email\", \"newField\"],\n    select: [\"name\", \"status\"]\n}\n\n// Execution Steps:\n1. transform:\n   {\n       name: \"John Doe\",\n       email: \"john@example.com\",\n       age: 30,\n       status: \"inactive\",\n       newField: \"added\"\n   }\n2. exclude:\n   {\n       name: \"John Doe\",\n       age: 30,\n       status: \"inactive\"\n   }\n3. select:\n   {\n       name: \"John Doe\",\n       status: \"inactive\"\n   }\n\n// Final Result:\n{\n    name: \"John Doe\",\n    status: \"inactive\"\n}\n</code></pre>"},{"location":"api/find_opts/#edge-cases-and-important-notes","title":"Edge Cases and Important Notes","text":""},{"location":"api/find_opts/#nested-paths-with-arrays","title":"Nested Paths with Arrays","text":"<p>When using dotted paths or array paths in <code>select</code> / <code>exclude</code>, paths do not traverse through arrays. If a path segment points to an array, it will be replaced with an empty object:</p> <pre><code>// Original Object\n{\n    users: [\n        { name: \"Alice\", age: 30 },\n        { name: \"Bob\", age: 25 }\n    ]\n}\n\n// Find Options\n{\n    select: [\"users.name\"]\n}\n\n// Result - array is replaced with object!\n{\n    users: {\n        name: undefined  // array was replaced\n    }\n}\n</code></pre> <p>To work with array elements, use array paths with specific indices:</p> <pre><code>{\n    select: [[\"users\", \"0\", \"name\"]]\n}\n</code></pre>"},{"location":"api/find_opts/#empty-select-vs-empty-exclude","title":"Empty Select vs Empty Exclude","text":"<ul> <li><code>select: []</code> returns an empty object <code>{}</code></li> <li><code>exclude: []</code> returns the original object unchanged</li> </ul>"},{"location":"api/find_opts/#transform-function-return-value","title":"Transform Function Return Value","text":"<p>The <code>transform</code> function can either:</p> <ol> <li>Modify and return the object (creates new reference)</li> <li>Mutate in place and return <code>undefined</code> (keeps original reference)</li> </ol> <pre><code>// Option 1: Return new object\n{ transform: (doc) =&gt; ({ ...doc, modified: true }) }\n\n// Option 2: Mutate in place\n{ transform: (doc) =&gt; { doc.modified = true; } }\n</code></pre>"},{"location":"api/find_opts/#select-and-exclude-interaction","title":"Select and Exclude Interaction","text":"<p>When both <code>select</code> and <code>exclude</code> are used:</p> <ol> <li><code>exclude</code> is applied first (removes fields from the object)</li> <li><code>select</code> is applied second (creates object with only selected fields from the remaining)</li> </ol> <pre><code>// Original: { a: 1, b: 2, c: 3, d: 4 }\n{\n    exclude: [\"b\"],            // Step 1: { a: 1, c: 3, d: 4 }\n    select: [\"a\", \"c\"]         // Step 2: { a: 1, c: 3 }\n}\n</code></pre> <p>The full execution order is always: <code>transform</code> -&gt; <code>exclude</code> -&gt; <code>select</code>.</p>"},{"location":"api/find_opts/#non-existent-paths","title":"Non-Existent Paths","text":"<ul> <li>Selecting non-existent paths silently returns <code>undefined</code> for that field</li> <li>Excluding non-existent paths has no effect (no error thrown)</li> </ul>"},{"location":"api/forge/","title":"Forge (Proxy Access)","text":"<p>Forge creates a Proxy that enables <code>db.collectionName.method()</code> syntax.</p>"},{"location":"api/forge/#forgetypedvaltherattarget","title":"<code>forgeTypedValthera&lt;T&gt;(target)</code>","text":"<p>Creates a typed proxy with TypeScript support. Accessing a property that doesn't exist on the target creates a <code>Collection</code> instance for that collection name.</p> <pre><code>import { forgeTypedValthera } from \"@wxn0brp/db-core\";\nimport { ValtheraClass } from \"@wxn0brp/db-core\";\n\ninterface User {\n  _id: string;\n  name: string;\n  email: string;\n}\n\nconst db = forgeTypedValthera&lt;{ users: User[] }&gt;(\n  new ValtheraClass({ adapter })\n);\n\n// db.users is typed as Collection&lt;User&gt;\nconst user = await db.users.findOne({ email: \"alice@example.com\" });\n// user is typed as User | null\n</code></pre>"},{"location":"api/forge/#how-it-works","title":"How it works","text":"<ul> <li>Accessing a non-existent property creates a <code>Collection</code> instance</li> <li>The collection is cached on the target object (subsequent accesses return the same instance)</li> <li>Existing properties (like <code>init</code>, <code>close</code>, <code>plugin</code>) are not affected</li> </ul> <pre><code>// Without forge - explicit collection access:\nconst users = db.c(\"users\");\nawait users.add({ name: \"Alice\" });\n\n// With forge - property access syntax:\nawait db.users.add({ name: \"Alice\" });\n</code></pre> <p>Note: <code>ValtheraCreate()</code> and <code>VDB()</code> automatically use <code>forgeTypedValthera</code> internally.</p>"},{"location":"api/id/","title":"ID Generation","text":"<p>ValtheraDB generates unique IDs using a time-based algorithm with random components.</p>"},{"location":"api/id/#id-format","title":"ID Format","text":"<p>Default format: <code>{timestamp36}-{random1}-{random1}</code></p> <ul> <li>timestamp36: Current time in base36 (milliseconds since epoch)</li> <li>random parts: Base36 random values with collision avoidance</li> </ul> <p>Example: <code>m1abc23-x-y</code></p>"},{"location":"api/id/#utility-functions","title":"Utility Functions","text":""},{"location":"api/id/#genidparts-iddata","title":"<code>genId(parts?, idData?)</code>","text":"<p>Generates a unique ID.</p> <p>Parameters:</p> <ul> <li><code>parts</code> (optional): Array of part lengths (default: <code>[1, 1]</code>)</li> <li><code>idData</code> (optional): State object for collision tracking</li> </ul> <pre><code>import { genId } from \"@wxn0brp/db-core\";\n\nconst id = genId(); // \"m1abc23-x-y\"\nconst customId = genId([2, 3]); // \"m1abc23-xy-zab\"\n</code></pre>"},{"location":"api/id/#convertidtounixid","title":"<code>convertIdToUnix(id)</code>","text":"<p>Extracts Unix timestamp (milliseconds) from an ID.</p> <pre><code>import { convertIdToUnix } from \"@wxn0brp/db-core\";\n\nconst id = \"m1abc23-x-y\";\nconst timestamp = convertIdToUnix(id); // 1700000000000\nconst date = new Date(timestamp);\n</code></pre>"},{"location":"api/id/#sortbyidsitems-key","title":"<code>sortByIds(items, key?)</code>","text":"<p>Sorts an array of objects by their ID field chronologically. Returns a new sorted array (does not mutate the original).</p> <ul> <li>Parameters:</li> <li><code>items</code> (<code>T[]</code>): Array of objects to sort.</li> <li><code>key</code> (<code>string</code>, optional, default: <code>\"_id\"</code>): The field name to sort by.</li> </ul> <pre><code>import { sortByIds } from \"@wxn0brp/db-core\";\n\nconst items = [\n  { _id: \"m2def45-a-b\", name: \"Second\" },\n  { _id: \"m1abc23-x-y\", name: \"First\" }\n];\n\nconst sorted = sortByIds(items);\n// [{ _id: \"m1abc23-x-y\", name: \"First\" }, { _id: \"m2def45-a-b\", name: \"Second\" }]\n\n// Using a custom ID key:\nconst sorted2 = sortByIds(items, \"id\");\n</code></pre>"},{"location":"api/id/#compareidsa-b","title":"<code>compareIds(a, b)</code>","text":"<p>Compares two IDs (string or number).</p> <pre><code>import { compareIds } from \"@wxn0brp/db-core\";\n\nconst result = compareIds(\"m1abc23-x-y\", \"m2def45-a-b\");\n// Negative if first is older, positive if newer, 0 if equal\n</code></pre>"},{"location":"api/id/#custom-id-generation","title":"Custom ID Generation","text":"<p>Disable auto-generation and provide your own IDs:</p> <pre><code>// Disable ID generation\nawait db.users.add(\n  { name: \"Alice\" },\n  false // id_gen = false\n);\n\n// Or use custom ID in updateOneOrAdd\nawait db.users.updateOneOrAdd(\n  { email: \"alice@example.com\" },\n  { name: \"Alice\" },\n  { add_arg: { _id: \"custom-id\" } }\n);\n</code></pre>"},{"location":"api/id/#custom-id-key","title":"Custom ID Key","text":"<p>By default, ValtheraDB uses <code>_id</code> as the ID field. You can configure a custom ID key via <code>adapterOpts</code>:</p> <pre><code>import { ValtheraCreate } from \"@wxn0brp/db\";\n\nconst db = ValtheraCreate(\"./my-db\", {\n  adapterOpts: {\n    idKey: \"id\", // Use \"id\" instead of \"_id\"\n  }\n});\n\n// Now all documents will use \"id\" as the ID field\nconst user = await db.users.add({ name: \"Alice\" });\n// { name: \"Alice\", id: \"m1abc23-x-y\" }\n</code></pre> <p>Note: The custom <code>idKey</code> applies to all collections in the database. When using <code>numberId: true</code>, the numeric auto-incrementing ID will also use the custom key.</p>"},{"location":"api/memory/","title":"In-Memory Database","text":"<p>ValtheraDB provides a built-in in-memory storage adapter for testing, prototyping, and browser environments.</p>"},{"location":"api/memory/#creatememoryvaltheratdata","title":"<code>createMemoryValthera&lt;T&gt;(data?)</code>","text":"<p>Factory function that creates a typed in-memory database, optionally pre-populated with data.</p> <p>Parameters:</p> <ul> <li><code>data</code> (optional): Object mapping collection names to arrays of documents</li> </ul> <p>Returns: <code>ValtheraMemory</code> with typed collection access</p> <p>Example with pre-populated data:</p> <pre><code>import { createMemoryValthera } from \"@wxn0brp/db-core\";\n\nconst db = createMemoryValthera({\n  users: [\n    { _id: \"user1\", name: \"Alice\", email: \"alice@example.com\" },\n    { _id: \"user2\", name: \"Bob\", email: \"bob@example.com\" }\n  ],\n  posts: [\n    { _id: \"post1\", title: \"Hello\", authorId: \"user1\" }\n  ]\n});\n\n// Data is immediately available\nconst users = await db.users.find({});\nconsole.log(users.length); // 2\n</code></pre>"},{"location":"api/memory/#how-it-works","title":"How It Works","text":"<p>The in-memory adapter uses a <code>Map&lt;string, any[]&gt;</code> internally. All data is isolated using <code>structuredClone</code> on read and write, ensuring that external mutations don't affect the database and vice versa.</p> <pre><code>const db = createMemoryValthera();\nawait db.users.add({ name: \"Alice\" });\n\n// External mutation does NOT affect stored data\nconst user = await db.users.findOne({});\nuser.name = \"Bob\"; // mutation\nconst stored = await db.users.findOne({});\nconsole.log(stored.name); // still \"Alice\" - structuredClone isolation\n</code></pre>"},{"location":"api/memory/#use-cases","title":"Use Cases","text":""},{"location":"api/memory/#unit-testing","title":"Unit Testing","text":"<pre><code>import { createMemoryValthera } from \"@wxn0brp/db-core\";\nimport { describe, it, expect } from \"bun:test\";\n\ndescribe(\"UserService\", () =&gt; {\n  it(\"should create a user\", async () =&gt; {\n    const db = createMemoryValthera();\n    const user = await db.users.add({ name: \"Alice\" });\n\n    expect(user._id).toBeDefined();\n    expect(user.name).toBe(\"Alice\");\n  });\n\n  it(\"should find users by email\", async () =&gt; {\n    const db = createMemoryValthera({\n      users: [\n        { _id: \"1\", name: \"Alice\", email: \"alice@test.com\" },\n        { _id: \"2\", name: \"Bob\", email: \"bob@test.com\" }\n      ]\n    });\n\n    const found = await db.users.findOne({ email: \"alice@test.com\" });\n    expect(found?.name).toBe(\"Alice\");\n  });\n});\n</code></pre>"},{"location":"api/memory/#browserpwa-applications","title":"Browser/PWA Applications","text":"<pre><code>// In a browser environment\nconst db = createMemoryValthera();\n\n// Store temporary session data\nawait db.session.add({\n  userId: \"123\",\n  token: \"abc\",\n  expiresAt: Date.now() + 3600000\n});\n\n// Data persists during the session\nconst session = await db.session.findOne({ userId: \"123\" });\n</code></pre> <p>For persistent storage, use a file-based adapter like <code>dir</code>.</p>"},{"location":"api/multi_storage/","title":"Multi-Backend &amp; Routed Storage","text":"<p>ValtheraDB provides two ways to work with multiple storage backends.</p>"},{"location":"api/multi_storage/#multibackend","title":"MultiBackend","text":"<p>Queries across multiple adapters simultaneously.</p> <pre><code>import { MultiBackend, ValtheraClass } from \"@wxn0brp/db-core\";\n\nconst multi = new MultiBackend([\n  adapter1,\n  adapter2,\n  adapter3,\n], 0); // primaryIndex (default: 0)\n\nconst db = new ValtheraClass({ adapter: multi });\n</code></pre> <p>Operation behavior:</p> Operation Behavior <code>add</code> Primary backend only <code>find</code> All backends, results merged (flattened) <code>findOne</code> Iterates backends, returns first non-null match <code>update</code> All backends, results merged <code>updateOne</code> Iterates backends, returns first non-null result <code>remove</code> All backends, results merged <code>removeOne</code> Iterates backends, returns first non-null result <code>ensureCollection</code> All backends <code>issetCollection</code> Returns <code>true</code> if any backend has the collection <code>removeCollection</code> All backends <code>getCollections</code> Unique union from all backends <p>Use cases: replication, migration, read replicas</p>"},{"location":"api/multi_storage/#routedstorage","title":"RoutedStorage","text":"<p>Routes collections to specific backends based on rules.</p> <pre><code>import { RoutedStorage, ValtheraClass } from \"@wxn0brp/db-core\";\n\nconst routed = new RoutedStorage([\n  { match: \"logs\", backends: logAdapter },\n  { match: /^temp_/, backends: memoryAdapter },\n  { match: (q) =&gt; q.collection.includes(\"archive\"), backends: archiveAdapter }\n], defaultAdapter);\n\nconst db = new ValtheraClass({ adapter: routed });\n</code></pre> <p>Match types:</p> <ul> <li>String: exact collection name or <code>\"*\"</code> for all</li> <li>RegExp: pattern match on collection name</li> <li>Function: <code>(query) =&gt; boolean</code></li> </ul> <p>Operation behavior:</p> Operation Behavior <code>add</code> Writes to first matched backend, then replicates to all matched backends <code>find</code> First matched backend only <code>findOne</code> First matched backend only <code>update</code> All matched backends <code>updateOne</code> All matched backends <code>remove</code> All matched backends <code>removeOne</code> All matched backends <code>getCollections</code> Unique union from all rule backends <p>When no rules match, the <code>defaultBackend</code> is used.</p> <p>Use cases: hot/cold storage, per-collection backends, multi-tenant</p>"},{"location":"api/multi_storage/#comparison","title":"Comparison","text":"Feature MultiBackend RoutedStorage Routing All backends for reads Per-collection by rules Writes Primary only All matching backends Use case Fan-out queries Collection-specific"},{"location":"api/relation/","title":"Relation Class Documentation","text":"<p>The <code>Relation</code> class provides a mechanism to handle relationships between collections in a database. It supports one-to-one, one-to-many, and many-to-many relationships.</p>"},{"location":"api/relation/#class-relation","title":"Class: <code>Relation</code>","text":""},{"location":"api/relation/#constructor-relationdbs","title":"Constructor: <code>Relation(dbs)</code>","text":"<p>Creates a new instance of the <code>Relation</code> class.</p> <ul> <li>Parameters:</li> <li><code>dbs</code> (<code>RelationTypes.DBS</code>): An object mapping database names to <code>ValtheraClass</code> or compatible instances.</li> </ul>"},{"location":"api/relation/#async-findonepath-search-relations-select","title":"<code>async findOne(path, search, relations, select?)</code>","text":"<p>Finds a single entry in a collection and resolves its relations.</p> <ul> <li>Parameters:</li> <li><code>path</code> (<code>RelationTypes.Path</code>): A tuple <code>[dbName, collectionName]</code> specifying the database and collection.</li> <li><code>search</code> (<code>Search</code>): The search criteria.</li> <li><code>relations</code> (<code>RelationTypes.Relation</code>): The relations to resolve.</li> <li> <p><code>select</code> (<code>string[][] | Record&lt;string, any&gt;</code>, optional): Fields to include in the result. Accepts array of paths or an object with paths as values.</p> </li> <li> <p>Returns:</p> </li> <li><code>Promise&lt;Object | null&gt;</code>: The found entry with resolved relations, or <code>null</code> if no match is found.</li> </ul>"},{"location":"api/relation/#async-findpath-search-relations-select-dbfindopts","title":"<code>async find(path, search, relations, select?, dbFindOpts?)</code>","text":"<p>Finds multiple entries in a collection and resolves their relations.</p> <ul> <li>Parameters:</li> <li><code>path</code> (<code>RelationTypes.Path</code>): A tuple <code>[dbName, collectionName]</code> specifying the database and collection.</li> <li><code>search</code> (<code>Search</code>): The search criteria.</li> <li><code>relations</code> (<code>RelationTypes.Relation</code>): The relations to resolve.</li> <li><code>select</code> (<code>string[][] | Record&lt;string, any&gt;</code>, optional): Fields to include in the results.</li> <li> <p><code>dbFindOpts</code> (<code>DbFindOpts</code>, optional): Options for the find operation (pagination, sorting, etc.).</p> </li> <li> <p>Returns:</p> </li> <li><code>Promise&lt;Object[]&gt;</code>: An array of found entries with resolved relations.</li> </ul>"},{"location":"api/relation/#relation-types","title":"Relation Types","text":"<p>The <code>Relation</code> class supports the following relation types:</p>"},{"location":"api/relation/#1-one-to-one-batch","title":"<code>\"1\"</code> - One-to-One (Batch)","text":"<p>Resolves a single related entry per document using a batched <code>$in</code> query. This is the most performant one-to-one mode.</p> <pre><code>{\n  author: {\n    path: [\"main\", \"users\"],\n    pk: \"_id\",        // field on the source document\n    fk: \"_id\",        // field on the related document\n    type: \"1\"\n  }\n}\n</code></pre>"},{"location":"api/relation/#11-one-to-one-individual","title":"<code>\"11\"</code> - One-to-One (Individual)","text":"<p>Similar to <code>\"1\"</code>, but internally performs a <code>findOne</code> operation for each document during a <code>find</code> query. Less performant but useful with custom adapters or legacy systems.</p> <pre><code>{\n  author: {\n    path: [\"main\", \"users\"],\n    pk: \"_id\",\n    fk: \"authorId\",\n    type: \"11\"\n  }\n}\n</code></pre>"},{"location":"api/relation/#1n-one-to-many","title":"<code>\"1n\"</code> - One-to-Many","text":"<p>Resolves multiple related entries. Supports <code>dbFindOpts</code> for sorting/pagination of related items.</p> <pre><code>{\n  posts: {\n    path: [\"main\", \"posts\"],\n    pk: \"_id\",\n    fk: \"authorId\",\n    type: \"1n\",\n    dbFindOpts: { limit: 10, sortBy: \"createdAt\", sortAsc: false }\n  }\n}\n</code></pre>"},{"location":"api/relation/#nm-many-to-many","title":"<code>\"nm\"</code> - Many-to-Many","text":"<p>Resolves all entries via a pivot/join table. Requires the <code>through</code> option.</p> <pre><code>{\n  tags: {\n    path: [\"main\", \"tags\"],\n    pk: \"_id\",\n    fk: \"_id\",\n    type: \"nm\",\n    through: {\n      table: \"post_tags\",  // pivot collection name\n      pk: \"postId\",       // pivot field referencing source\n      fk: \"tagId\",        // pivot field referencing target\n      db: \"main\"          // optional: database name for pivot (defaults to source db)\n    }\n  }\n}\n</code></pre>"},{"location":"api/relation/#configuration-options","title":"Configuration Options","text":"<p>Each relation entry supports these options:</p> Option Type Default Description <code>path</code> <code>[string, string]</code> required <code>[dbName, collectionName]</code> of the related data <code>pk</code> <code>string</code> <code>\"_id\"</code> Field on the source document to match <code>fk</code> <code>string</code> <code>\"_id\"</code> Field on the related document to match <code>type</code> <code>\"1\" \\| \"11\" \\| \"1n\" \\| \"nm\"</code> <code>\"1\"</code> Relationship type <code>as</code> <code>string</code> relation key name Field name in the result object <code>select</code> <code>string[]</code> <code>undefined</code> Fields to include from related documents <code>dbFindOpts</code> <code>DbFindOpts</code> <code>{}</code> Options for <code>\"1n\"</code> type (sorting, pagination) <code>through</code> <code>Object</code> <code>undefined</code> Pivot table config for <code>\"nm\"</code> type <code>relations</code> <code>Relation</code> <code>undefined</code> Nested relations to resolve on related documents"},{"location":"api/relation/#nested-relations","title":"Nested Relations","text":"<p>Relations can be nested to resolve deeply linked data:</p> <pre><code>const relations = {\n  author: {\n    path: [\"main\", \"users\"],\n    pk: \"_id\",\n    fk: \"_id\",\n    type: \"1\",\n    relations: {\n      // Resolve relations on the author document itself\n      recentPosts: {\n        path: [\"main\", \"posts\"],\n        pk: \"_id\",\n        fk: \"authorId\",\n        type: \"1n\"\n      }\n    }\n  },\n  comments: {\n    path: [\"main\", \"comments\"],\n    pk: \"_id\",\n    fk: \"postId\",\n    type: \"1n\",\n    relations: {\n      // Resolve relations on each comment\n      author: {\n        path: [\"main\", \"users\"],\n        pk: \"authorId\",\n        fk: \"_id\",\n        type: \"1\"\n      }\n    }\n  }\n};\n</code></pre>"},{"location":"api/relation/#the-as-option","title":"The <code>as</code> Option","text":"<p>By default, the relation is stored under the key name used in the relations object. Use <code>as</code> to rename it:</p> <pre><code>const relations = {\n  writer: {  // key name\n    path: [\"main\", \"users\"],\n    pk: \"authorId\",\n    fk: \"_id\",\n    type: \"1\",\n    as: \"author\"  // stored as result.author, not result.writer\n  }\n};\n</code></pre>"},{"location":"api/relation/#the-select-option","title":"The <code>select</code> Option","text":"<p>Use <code>select</code> to limit which fields are included from related documents. The foreign key field is automatically added and removed to avoid duplication:</p> <pre><code>{\n  author: {\n    path: [\"main\", \"users\"],\n    pk: \"_id\",\n    fk: \"_id\",\n    type: \"1\",\n    select: [\"name\", \"email\"]  // only these fields are returned\n  }\n}\n</code></pre> <p><code>select</code> accepts an array of field paths. You can also pass an object to <code>findOne</code>/<code>find</code> as the <code>select</code> parameter for result-level field selection:</p> <pre><code>const post = await relation.findOne(\n  [\"main\", \"posts\"],\n  { _id: \"post123\" },\n  relations,\n  { title: true, author: { name: true } }  // object form\n);\n</code></pre>"},{"location":"api/relation/#example-usage","title":"Example Usage","text":"<pre><code>import { Relation } from \"@wxn0brp/db-core\";\nimport { ValtheraCreate } from \"@wxn0brp/db\";\n\n// Create database instances\nconst db1 = ValtheraCreate(\"./db1\");\nconst db2 = ValtheraCreate(\"./db2\");\n\n// Define databases\nconst dbs = {\n    db1,\n    db2\n};\n\n// Define relations\nconst relations = {\n    author: {\n        path: [\"db1\", \"users\"],\n        pk: \"authorId\",\n        fk: \"_id\",\n        type: \"1\",\n        select: [\"name\", \"email\"]\n    },\n    comments: {\n        path: [\"db2\", \"comments\"],\n        pk: \"_id\",\n        fk: \"postId\",\n        type: \"1n\",\n        dbFindOpts: { limit: 20, sortBy: \"createdAt\", sortAsc: false }\n    }\n};\n\n// Create a Relation instance\nconst relation = new Relation(dbs);\n\n// Find a single post with relations\nconst post = await relation.findOne(\n    [\"db1\", \"posts\"],\n    { _id: \"post123\" },\n    relations,\n    [[\"title\"], [\"author.name\"], [\"comments.content\"]]\n);\n\nconsole.log(post);\n\n// Find multiple posts with relations\nconst posts = await relation.find(\n    [\"db1\", \"posts\"],\n    { category: \"tech\" },\n    relations,\n    [[\"title\"], [\"author.name\"], [\"comments.content\"]],\n    { limit: 10 }\n);\n\nconsole.log(posts);\n</code></pre>"},{"location":"api/remote/","title":"Remote Valthera and Graph Valthera Client Documentation","text":""},{"location":"api/remote/#remote-object-structure","title":"<code>remote</code> Object Structure","text":"<ul> <li><code>name</code> (<code>string</code>): The name of the Valthera.</li> <li><code>url</code> (<code>string</code>): The URL of the remote Valthera.</li> <li><code>auth</code> (<code>string</code>): The authentication token for accessing the Valthera.</li> </ul>"},{"location":"api/remote/#class-valtheraremoteremote","title":"Class: <code>ValtheraRemote(remote)</code>","text":"<p><code>ValtheraRemote</code> is an extended version of the <code>Valthera</code> class, designed to handle API requests. It provides the same functionalities as <code>Valthera</code>, but enables remote communication, allowing you to interact with Valthera through HTTP requests.</p>"},{"location":"api/remote/#example-usage","title":"Example Usage","text":"<p><pre><code>const remoteDB = new ValtheraRemote({\n    name: 'myRemoteDB',\n    url: 'https://example.com/db',\n    auth: 'your-auth-token'\n});\n</code></pre> or <pre><code>const remoteDB = new ValtheraRemote('https://dbName:token@example.com/db');\n</code></pre></p>"},{"location":"api/search_opts/","title":"Search Operators","text":""},{"location":"api/search_opts/#operators","title":"Operators","text":""},{"location":"api/search_opts/#logical-operators","title":"Logical Operators","text":""},{"location":"api/search_opts/#and","title":"$and","text":"<p>Checks if all conditions in an array are true. <pre><code>{\n  $and: [\n    { $gt: { age: 20 } },\n    { $exists: { name: true } }\n  ]\n}\n</code></pre></p>"},{"location":"api/search_opts/#or","title":"$or","text":"<p>Checks if at least one condition in an array is true. <pre><code>{\n  $or: [\n    { $lt: { age: 20 } },\n    { $gt: { age: 60 } }\n  ]\n}\n</code></pre></p>"},{"location":"api/search_opts/#not","title":"$not","text":"<p>Negates a condition. <pre><code>{\n  $not: { $type: { age: \"string\" } }\n}\n</code></pre></p>"},{"location":"api/search_opts/#comparison-operators","title":"Comparison Operators","text":""},{"location":"api/search_opts/#ne","title":"$ne","text":"<p>Not equal comparison. <pre><code>{ $ne: { status: \"banned\" } }\n</code></pre></p>"},{"location":"api/search_opts/#gt","title":"$gt","text":"<p>Greater than comparison. <pre><code>{ $gt: { age: 18 } }\n</code></pre></p>"},{"location":"api/search_opts/#lt","title":"$lt","text":"<p>Less than comparison. <pre><code>{ $lt: { score: 100 } }\n</code></pre></p>"},{"location":"api/search_opts/#gte","title":"$gte","text":"<p>Greater than or equal comparison. <pre><code>{ $gte: { price: 9.99 } }\n</code></pre></p>"},{"location":"api/search_opts/#lte","title":"$lte","text":"<p>Less than or equal comparison. <pre><code>{ $lte: { quantity: 50 } }\n</code></pre></p>"},{"location":"api/search_opts/#idgt","title":"$idGt","text":"<p>Greater than comparison for IDs (accepts <code>string</code> or <code>number</code>). <pre><code>{ $idGt: { _id: \"miblu25k-d-g\" } }\n</code></pre></p>"},{"location":"api/search_opts/#idlt","title":"$idLt","text":"<p>Less than comparison for IDs (accepts <code>string</code> or <code>number</code>). <pre><code>{ $idLt: { _id: \"miblu25k-d-g\" } }\n</code></pre></p>"},{"location":"api/search_opts/#idgte","title":"$idGte","text":"<p>Greater than or equal comparison for IDs (accepts <code>string</code> or <code>number</code>). <pre><code>{ $idGte: { _id: \"miblu25k-d-g\" } }\n</code></pre></p>"},{"location":"api/search_opts/#idlte","title":"$idLte","text":"<p>Less than or equal comparison for IDs (accepts <code>string</code> or <code>number</code>). <pre><code>{ $idLte: { _id: \"miblu25k-d-g\" } }\n</code></pre></p>"},{"location":"api/search_opts/#in","title":"$in","text":"<p>Checks if value is in an array. <pre><code>{ $in: { status: [\"active\", \"pending\"] } }\n</code></pre></p>"},{"location":"api/search_opts/#nin","title":"$nin","text":"<p>Checks if value is not in an array. <pre><code>{ $nin: { category: [\"archived\", \"deleted\"] } }\n</code></pre></p>"},{"location":"api/search_opts/#between","title":"$between","text":"<p>Checks if a number is between two values (inclusive). <pre><code>{ $between: { age: [18, 65] } }\n</code></pre></p>"},{"location":"api/search_opts/#type-and-existence-operators","title":"Type and Existence Operators","text":""},{"location":"api/search_opts/#exists","title":"$exists","text":"<p>Checks if a field exists (or doesn't exist). <pre><code>{ $exists: { email: true, deletedAt: false } }\n</code></pre></p>"},{"location":"api/search_opts/#type","title":"$type","text":"<p>Checks the type of a field. <pre><code>{ $type: { age: \"number\", name: \"string\" } }\n</code></pre></p>"},{"location":"api/search_opts/#array-operators","title":"Array Operators","text":""},{"location":"api/search_opts/#arrinc","title":"$arrinc","text":"<p>Checks if an array includes at least one of the specified values. <pre><code>{ $arrinc: { tags: [\"developer\", \"designer\"] } }\n</code></pre></p>"},{"location":"api/search_opts/#arrincall","title":"$arrincall","text":"<p>Checks if an array includes all of the specified values. <pre><code>{ $arrincall: { permissions: [\"read\", \"write\"] } }\n</code></pre></p>"},{"location":"api/search_opts/#size","title":"$size","text":"<p>Checks the length of an array or string. <pre><code>{ $size: { tags: 3 } }\n</code></pre></p>"},{"location":"api/search_opts/#string-operators","title":"String Operators","text":""},{"location":"api/search_opts/#regex","title":"$regex","text":"<p>Tests a string against a regular expression. <pre><code>{ $regex: { email: /^[^@]+@[^@]+\\.[^@]+$/ } }\n</code></pre></p>"},{"location":"api/search_opts/#startswith","title":"$startsWith","text":"<p>Checks if a string starts with a specified value. <pre><code>{ $startsWith: { name: \"Dr.\" } }\n</code></pre></p>"},{"location":"api/search_opts/#endswith","title":"$endsWith","text":"<p>Checks if a string ends with a specified value. <pre><code>{ $endsWith: { email: \"@example.com\" } }\n</code></pre></p>"},{"location":"api/search_opts/#istartswith","title":"$iStartsWith","text":"<p>Checks if a string starts with a specified value (case-insensitive). <pre><code>{ $iStartsWith: { name: \"dr\" } } // matches \"Dr.\", \"DR.\", \"dr.\"\n</code></pre></p>"},{"location":"api/search_opts/#iendswith","title":"$iEndsWith","text":"<p>Checks if a string ends with a specified value (case-insensitive). <pre><code>{ $iEndsWith: { email: \"@EXAMPLE.COM\" } } // matches \"@example.com\", \"@Example.Com\"\n</code></pre></p>"},{"location":"api/search_opts/#other-operators","title":"Other Operators","text":""},{"location":"api/search_opts/#subset","title":"$subset","text":"<p>Disables advanced operator parsing for the wrapped fields, treating <code>$</code>-prefixed keys as literal field names. Use this when your data contains keys that start with <code>$</code> (like MongoDB-style fields) and you want to match them literally rather than having them interpreted as operators.</p> <pre><code>// Without $subset: $lt would be interpreted as \"less than\" operator\n// With $subset: $lt is treated as a literal field name\n{ $subset: { $lt: \"John Doe\" } } // checks if the field \"$lt\" equals \"John Doe\"\n</code></pre>"},{"location":"api/search_opts/#function-based-search","title":"Function-Based Search","text":"<p>Instead of using operator objects, you can provide a function for fully custom search logic:</p> <pre><code>// Search function signature:\n(data: T, context: VContext) =&gt; boolean\n\n// Example: find users older than 20\nconst adults = await db.users.find(\n  (user, ctx) =&gt; user.age &gt; 20 &amp;&amp; user.status === \"active\"\n);\n\n// Example: using context to pass external state\nconst threshold = 18;\nconst adults = await db.users.find(\n  (user, ctx) =&gt; user.age &gt;= ctx.threshold,\n  {},  // dbFindOpts\n  {},  // findOpts\n  { threshold }  // context\n);\n</code></pre> <p>The function receives the document and a context object, and returns <code>true</code> to include the document in results.</p>"},{"location":"api/search_opts/#examples","title":"Examples","text":""},{"location":"api/search_opts/#complex-validation","title":"Complex Validation","text":"<pre><code>const criteria = {\n  $and: [\n    {\n      $or: [\n        { $gt: { age: 18 } },\n        { $exists: { guardianConsent: true } }\n      ]\n    },\n    {\n      $type: { email: \"string\" },\n      $regex: { email: /^[^@]+@[^@]+\\.[^@]+$/ }\n    },\n    {\n      $arrincall: { roles: [\"user\"] },\n      $not: { $in: { status: [\"banned\", \"suspended\"] } }\n    }\n  ]\n};\n\nconst user = {\n  age: 16,\n  guardianConsent: true,\n  email: \"john@example.com\",\n  roles: [\"user\", \"premium\"],\n  status: \"active\"\n};\n\nconst isValid = hasFieldsAdvanced(user, criteria); // true\n</code></pre>"},{"location":"api/search_opts/#nested-conditions","title":"Nested Conditions","text":"<pre><code>const criteria = {\n  $and: [\n    {\n      $exists: { address: true },\n      $type: { address: \"object\" }\n    },\n    {\n      $or: [\n        { $exists: { \"address.zipCode\": true } },\n        {\n          $and: [\n            { $exists: { \"address.city\": true } },\n            { $exists: { \"address.country\": true } }\n          ]\n        }\n      ]\n    }\n  ]\n};\n\nconst user = {\n  address: {\n    city: \"New York\",\n    country: \"USA\"\n  }\n};\n\nconst isValid = hasFieldsAdvanced(user, criteria); // true\n</code></pre>"},{"location":"api/search_opts/#error-handling","title":"Error Handling","text":"<p>The function will throw an error if: - The <code>fields</code> parameter is not an object - The <code>fields</code> parameter is null</p> <p>Always wrap the function call in a try-catch block when using with untrusted input:</p> <pre><code>try {\n  const isValid = hasFieldsAdvanced(obj, criteria);\n  // Handle result\n} catch (error) {\n  // Handle error\n  console.error('Validation error:', error.message);\n}\n</code></pre>"},{"location":"api/search_opts/#undefined-values","title":"Undefined Values","text":"<p>Properties with <code>undefined</code> values are treated as missing keys:</p> <pre><code>const obj = { a: undefined };\nconst criteria = { a: undefined };\nhasFieldsAdvanced(obj, criteria); // false - treated as missing key\n</code></pre> <p>If you need to check for <code>undefined</code> values, use <code>$exists</code> operator:</p> <pre><code>{ $exists: { a: false } } // checks if key 'a' does not exist\n</code></pre>"},{"location":"api/search_opts/#operator-execution-order","title":"Operator Execution Order","text":"<p>When using multiple operators including <code>$and</code> or <code>$or</code>, the execution order is:</p> <ol> <li>Basic field matching (non-<code>$</code> prefixed keys)</li> <li><code>$subset</code> operator</li> <li>Comparison operators (<code>$gt</code>, <code>$lt</code>, <code>$in</code>, etc.)</li> <li><code>$not</code> operator</li> <li><code>$and</code> / <code>$or</code> operators (these are evaluated last and return immediately)</li> </ol> <p>This means <code>$and</code>/<code>$or</code> can short-circuit the evaluation. For complex queries, structure your criteria accordingly.</p>"},{"location":"api/search_opts/#null-vs-undefined","title":"Null vs Undefined","text":"<ul> <li><code>null</code> values are matched with <code>{ field: null }</code></li> <li><code>undefined</code> values or missing keys require <code>$exists: { field: false }</code></li> </ul>"},{"location":"api/updater/","title":"Predefined Update Options Quick Reference","text":""},{"location":"api/updater/#arrays","title":"Arrays","text":""},{"location":"api/updater/#push","title":"<code>$push</code>","text":"<p>Adds an element to the end of an array.</p> <pre><code>{\n    $push: { tags: \"designer\" }\n}\n</code></pre> <p>Input:</p> <pre><code>{\n    tags: [\"developer\"]\n}\n</code></pre> <p>Output:</p> <pre><code>{\n    tags: [\"developer\", \"designer\"]\n}\n</code></pre>"},{"location":"api/updater/#pushset","title":"<code>$pushSet</code>","text":"<p>Adds an element to the end of an array and removes duplicates.</p> <pre><code>{\n    $pushSet: { tags: \"designer\" }\n}\n</code></pre> <p>Input:</p> <pre><code>{\n    tags: [\"developer\", \"designer\", \"developer\"]\n}\n</code></pre> <p>Output:</p> <pre><code>{\n    tags: [\"developer\", \"designer\"]\n}\n</code></pre>"},{"location":"api/updater/#pull","title":"<code>$pull</code>","text":"<p>Removes a specific element from an array.</p> <pre><code>{\n    $pull: { tags: \"developer\" }\n}\n</code></pre> <p>Input:</p> <pre><code>{\n    tags: [\"developer\", \"designer\"]\n}\n</code></pre> <p>Output:</p> <pre><code>{\n    tags: [\"designer\"]\n}\n</code></pre>"},{"location":"api/updater/#pullall","title":"<code>$pullAll</code>","text":"<p>Removes all occurrences of specified elements from an array.</p> <pre><code>{\n    $pullAll: { tags: [\"developer\", \"designer\"] }\n}\n</code></pre> <p>Input:</p> <pre><code>{\n    tags: [\"developer\", \"designer\", \"manager\"]\n}\n</code></pre> <p>Output:</p> <pre><code>{\n    tags: [\"manager\"]\n}\n</code></pre>"},{"location":"api/updater/#pushall","title":"<code>$pushAll</code>","text":"<p>Adds multiple elements to the end of an array.</p> <pre><code>{\n    $pushAll: { tags: [\"developer\", \"designer\"] }\n}\n</code></pre> <p>Input:</p> <pre><code>{\n    tags: [\"manager\"]\n}\n</code></pre> <p>Output:</p> <pre><code>{\n    tags: [\"manager\", \"developer\", \"designer\"]\n}\n</code></pre>"},{"location":"api/updater/#numbers","title":"Numbers","text":""},{"location":"api/updater/#inc","title":"<code>$inc</code>","text":"<p>Increments a numeric value by a given amount.</p> <pre><code>{\n    $inc: { counter: 1 }\n}\n</code></pre> <p>Input:</p> <pre><code>{\n    counter: 5\n}\n</code></pre> <p>Output:</p> <pre><code>{\n    counter: 6\n}\n</code></pre>"},{"location":"api/updater/#dec","title":"<code>$dec</code>","text":"<p>Decrements a numeric value by a given amount.</p> <pre><code>{\n    $dec: { counter: 1 }\n}\n</code></pre> <p>Input:</p> <pre><code>{\n    counter: 5\n}\n</code></pre> <p>Output:</p> <pre><code>{\n    counter: 4\n}\n</code></pre>"},{"location":"api/updater/#objects","title":"Objects","text":""},{"location":"api/updater/#merge","title":"<code>$merge</code>","text":"<p>Merges a nested object, adding or updating properties. For arrays, concatenates them.</p> <pre><code>{\n    $merge: { \n        settings: {\n            theme: \"dark\",\n            roles: [\"admin\"]\n        }\n    }\n}\n</code></pre> <p>Input:</p> <pre><code>{\n    settings: {\n        theme: \"light\",\n        language: \"en\",\n        roles: [\"user\"]\n    }\n}\n</code></pre> <p>Output:</p> <pre><code>{\n    settings: {\n        theme: \"dark\",\n        language: \"en\",\n        roles: [\"user\", \"admin\"]\n    }\n}\n</code></pre>"},{"location":"api/updater/#deepmerge","title":"<code>$deepMerge</code>","text":"<p>Deeply merges nested objects, adding or updating properties recursively.</p> <pre><code>{\n    $deepMerge: {\n        user: {\n            address: {\n                city: \"New York\"\n            }\n        }\n    }\n}\n</code></pre> <p>Input:</p> <pre><code>{\n    user: {\n        name: \"John\",\n        address: {\n            street: \"123 Main St\",\n            city: \"San Francisco\"\n        }\n    }\n}\n</code></pre> <p>Output:</p> <pre><code>{\n    user: {\n        name: \"John\",\n        address: {\n            street: \"123 Main St\",\n            city: \"New York\"\n        }\n    }\n}\n</code></pre>"},{"location":"api/updater/#others","title":"Others","text":""},{"location":"api/updater/#set","title":"<code>$set</code>","text":"<p>Sets a field to a specific value. Functionally identical to using a plain field assignment in the updater object.</p> <pre><code>{\n    $set: { name: \"John\" }\n}\n</code></pre> <p>Input:</p> <pre><code>{\n    name: \"Jane\",\n    age: 30\n}\n</code></pre> <p>Output:</p> <pre><code>{\n    name: \"John\",\n    age: 30\n}\n</code></pre> <p>Note: <code>{ $set: { name: \"John\" } }</code> is equivalent to <code>{ name: \"John\" }</code>.</p>"},{"location":"api/updater/#unset","title":"<code>$unset</code>","text":"<p>Removes a specified key from an object.</p> <pre><code>{\n    $unset: { age: true }\n}\n</code></pre> <p>Input:</p> <pre><code>{\n    name: \"John\",\n    age: 30\n}\n</code></pre> <p>Output:</p> <pre><code>{\n    name: \"John\"\n}\n</code></pre>"},{"location":"api/updater/#rename","title":"<code>$rename</code>","text":"<p>Renames a key in an object.</p> <pre><code>{\n    $rename: { firstName: \"name\" }\n}\n</code></pre> <p>Input:</p> <pre><code>{\n    firstName: \"John\",\n    lastName: \"Doe\"\n}\n</code></pre> <p>Output:</p> <pre><code>{\n    name: \"John\",\n    lastName: \"Doe\"\n}\n</code></pre>"},{"location":"api/updater/#function-based-updater","title":"Function-Based Updater","text":"<p>Instead of using operator objects, you can provide a function for fully custom update logic:</p> <pre><code>// Updater function signature:\n(data: T, context: VContext) =&gt; Data | void\n\n// Example: double the counter field\nawait db.users.updateOne(\n  { name: \"Alice\" },\n  (user, ctx) =&gt; ({ counter: user.counter * 2 })\n);\n\n// Example: using context for external values\nawait db.users.update(\n  { status: \"active\" },\n  (user, ctx) =&gt; ({ score: user.score + ctx.bonus }),\n  {},  // context\n);\n</code></pre> <p>The function receives the current document and a context object. It can either: 1. Return a new object with the fields to update 2. Mutate the document in place and return <code>void</code></p>"},{"location":"api/valthera/","title":"ValtheraDB Documentation","text":""},{"location":"api/valthera/#class-valthera","title":"Class: <code>Valthera</code>","text":""},{"location":"api/valthera/#constructor-options","title":"Constructor Options","text":"<pre><code>new ValtheraClass({\n  adapter: ActionsBase | (() =&gt; Promise&lt;ActionsBase&gt;),\n  executor?: Executor,\n  adapterOpts?: AdapterOpts,\n  // Legacy (deprecated):\n  numberId?: boolean,   // use adapterOpts.numberId\n  idKey?: string,       // use adapterOpts.idKey\n})\n</code></pre> <p>The <code>adapter</code> can be either an <code>ActionsBase</code> instance or a factory function that returns one. When a factory function is used, it is called lazily during <code>init()</code>.</p>"},{"location":"api/valthera/#adapteropts","title":"<code>AdapterOpts</code>","text":"<p>Configuration options for the storage adapter.</p> <pre><code>interface AdapterOpts {\n  numberId?: boolean;  // Use numeric auto-incrementing IDs (default: false)\n  idKey?: string;      // Custom ID field name (default: \"_id\")\n}\n</code></pre> <p>Example:</p> <pre><code>const db = new ValtheraClass({\n  adapter: myAdapter,\n  adapterOpts: {\n    numberId: true,   // Use numeric IDs (1, 2, 3...)\n    idKey: \"id\",      // Use \"id\" instead of \"_id\"\n  }\n});\n</code></pre> <p>Note: The legacy <code>numberId</code> and <code>idKey</code> options at the top level are deprecated. Use <code>adapterOpts</code> instead.</p>"},{"location":"api/valthera/#version","title":"<code>version</code>","text":"<p>The version of the ValtheraDB core engine.</p> <pre><code>db.version; // e.g., \"0.11.6\"\n</code></pre>"},{"location":"api/valthera/#async-initargs","title":"<code>async init(...args)</code>","text":"<p>Initializes the adapter. Must be called before performing any operations. If the adapter was provided as a factory function, it is called during this step. Calling <code>init()</code> on an already-initialized instance is a no-op.</p> <pre><code>await db.init();\n</code></pre>"},{"location":"api/valthera/#async-closeargs","title":"<code>async close(...args)</code>","text":"<p>Closes the adapter connection. Subsequent operations will not work until <code>init()</code> is called again.</p> <pre><code>await db.close();\n</code></pre>"},{"location":"api/valthera/#ctcollection","title":"<code>c&lt;T&gt;(collection)</code>","text":"<p>Creates or retrieves a <code>Collection</code> instance for the given collection name. The collection is cached for reuse.</p> <ul> <li>Parameters:</li> <li><code>collection</code> (<code>string</code>): The name of the collection.</li> <li>Returns:</li> <li><code>Collection&lt;T&gt;</code>: A typed collection instance.</li> </ul> <pre><code>const users = db.c(\"users\");\nawait users.add({ name: \"Alice\" });\n</code></pre> <p>Note: When using <code>forgeTypedValthera()</code> or <code>ValtheraCreate()</code>, you can access collections directly as properties (e.g., <code>db.users</code>).</p>"},{"location":"api/valthera/#pluginplugin","title":"<code>plugin(plugin)</code>","text":"<p>Registers a plugin to intercept database operations. Plugins form a middleware chain that can inspect, modify, or short-circuit queries before they reach the adapter.</p> <ul> <li>Parameters:<ul> <li><code>plugin</code> (<code>ValtheraPlugin</code>): The plugin instance to register.</li> </ul> </li> <li>Returns:<ul> <li><code>() =&gt; void</code>: A function that unregisters the plugin when called.</li> </ul> </li> </ul> <p>Plugin Interface:</p> <pre><code>interface ValtheraPlugin {\n  name: string;\n  init?: (db: ValtheraClass) =&gt; void;\n  execute(ctx: PluginContext): Promise&lt;any&gt;;\n}\n\ninterface PluginContext {\n  op: string;        // Operation name (e.g., \"find\", \"add\", \"update\")\n  query: any;        // The query object or string\n  next: () =&gt; Promise&lt;any&gt;; // Call next plugin or adapter\n}\n</code></pre> <p>Example:</p> <pre><code>const loggingPlugin: ValtheraPlugin = {\n  name: \"logger\",\n  async execute(ctx) {\n    console.log(`Executing ${ctx.op}`);\n    const result = await ctx.next();\n    console.log(`Completed ${ctx.op}`);\n    return result;\n  }\n};\n\nconst unregister = db.plugin(loggingPlugin);\n// Later: unregister() to remove the plugin\n</code></pre>"},{"location":"api/valthera/#async-getcollections","title":"<code>async getCollections()</code>","text":"<p>Gets the names of all available collections in the database.</p> <ul> <li>Returns:<ul> <li><code>Promise&lt;string[]&gt;</code>: A promise that resolves with an array of collection names.</li> </ul> </li> </ul>"},{"location":"api/valthera/#async-ensurecollectioncollection","title":"<code>async ensureCollection(collection)</code>","text":"<p>Ensures that the specified collection exists. If the collection does not exist, it will be created.</p> <ul> <li>Parameters:<ul> <li><code>collection</code> (<code>string</code>): The name of the collection.</li> </ul> </li> <li>Returns:<ul> <li><code>Promise&lt;boolean&gt;</code>: The returned value is adapter-dependent and must not be relied upon.</li> </ul> </li> <li>Notes:<ul> <li>Guarantees that the collection exists after the method resolves.</li> <li>The boolean result has no standardized meaning and should be treated as <code>void</code>.</li> </ul> </li> </ul>"},{"location":"api/valthera/#async-issetcollectioncollection","title":"<code>async issetCollection(collection)</code>","text":"<p>Checks if a collection exists.</p> <ul> <li>Parameters:<ul> <li><code>collection</code> (<code>string</code>): The name of the collection.</li> </ul> </li> <li>Returns:<ul> <li><code>Promise&lt;boolean&gt;</code>: A promise that resolves to <code>true</code> if the collection exists, otherwise <code>false</code>.</li> </ul> </li> </ul>"},{"location":"api/valthera/#async-removecollectioncollection","title":"<code>async removeCollection(collection)</code>","text":"<p>Deletes the collection.</p> <ul> <li> <p>Parameters:</p> <ul> <li><code>collection</code> (<code>string</code>): The name of the collection to remove.</li> </ul> </li> <li> <p>Returns:</p> <ul> <li><code>Promise&lt;boolean&gt;</code>: A promise that resolves when the collection is removed.</li> <li><code>true</code>: The collection was successfully removed.</li> <li><code>false</code>: The collection was not found (i.e., it did not exist, so there was nothing to remove).</li> </ul> </li> </ul>"},{"location":"api/valthera/#addquery","title":"<code>add(query)</code>","text":"<p>Adds a document to a collection.</p> <ul> <li>Parameters:</li> <li><code>query</code> (<code>VQueryT.Add&lt;T&gt;</code>): The add query.</li> <li>Returns: <code>Promise&lt;T &amp; { _id: string }&gt;</code></li> </ul>"},{"location":"api/valthera/#findquery","title":"<code>find(query)</code>","text":"<p>Finds documents matching criteria.</p> <ul> <li>Parameters:</li> <li><code>query</code> (<code>VQueryT.Find&lt;T&gt;</code>): The find query.</li> <li>Returns: <code>Promise&lt;T[]&gt;</code></li> </ul>"},{"location":"api/valthera/#findonequery","title":"<code>findOne(query)</code>","text":"<p>Finds the first matching document.</p> <ul> <li>Parameters:</li> <li><code>query</code> (<code>VQueryT.FindOne&lt;T&gt;</code>): The findOne query.</li> <li>Returns: <code>Promise&lt;T | null&gt;</code></li> </ul>"},{"location":"api/valthera/#updatequery","title":"<code>update(query)</code>","text":"<p>Updates all matching documents.</p> <ul> <li>Parameters:</li> <li><code>query</code> (<code>VQueryT.Update&lt;T&gt;</code>): The update query.</li> <li>Returns: <code>Promise&lt;T[]&gt;</code></li> </ul>"},{"location":"api/valthera/#updateonequery","title":"<code>updateOne(query)</code>","text":"<p>Updates the first matching document.</p> <ul> <li>Parameters:</li> <li><code>query</code> (<code>VQueryT.Update&lt;T&gt;</code>): The update query.</li> <li>Returns: <code>Promise&lt;T | null&gt;</code></li> </ul>"},{"location":"api/valthera/#removequery","title":"<code>remove(query)</code>","text":"<p>Removes all matching documents.</p> <ul> <li>Parameters:</li> <li><code>query</code> (<code>VQueryT.Remove&lt;T&gt;</code>): The remove query.</li> <li>Returns: <code>Promise&lt;T[]&gt;</code></li> </ul>"},{"location":"api/valthera/#removeonequery","title":"<code>removeOne(query)</code>","text":"<p>Removes the first matching document.</p> <ul> <li>Parameters:</li> <li><code>query</code> (<code>VQueryT.Remove&lt;T&gt;</code>): The remove query.</li> <li>Returns: <code>Promise&lt;T | null&gt;</code></li> </ul>"},{"location":"api/valthera/#updateoneoraddquery","title":"<code>updateOneOrAdd(query)</code>","text":"<p>Updates one entry or adds a new one if no match is found.</p> <ul> <li>Parameters:</li> <li><code>query</code> (<code>VQueryT.UpdateOneOrAdd&lt;T&gt;</code>): The updateOneOrAdd query.</li> <li>Returns: <code>Promise&lt;{ data: T; type: \"added\" | \"updated\" }&gt;</code></li> </ul>"},{"location":"api/valthera/#toggleonequery","title":"<code>toggleOne(query)</code>","text":"<p>Removes one entry if it exists, or adds a new one if it doesn't. Useful for toggling flags.</p> <ul> <li>Parameters:</li> <li><code>query</code> (<code>VQueryT.ToggleOne&lt;T&gt;</code>): The toggleOne query.</li> <li>Returns: <code>Promise&lt;{ data: T; type: \"added\" | \"removed\" }&gt;</code></li> </ul>"},{"location":"api/valthera/#event-emitter","title":"Event Emitter","text":""},{"location":"api/valthera/#emitter","title":"<code>emitter</code>","text":"<p>Event emitter for subscribing to database operations. Uses VEE (Valthera Event Emitter).</p> <p>Type: <code>VEE&lt;Events&gt;</code></p> <p>Events: - Operation-specific events: <code>\"find\"</code>, <code>\"add\"</code>, <code>\"update\"</code>, <code>\"updateOne\"</code>, <code>\"remove\"</code>, <code>\"removeOne\"</code>, <code>\"findOne\"</code>, <code>\"updateOneOrAdd\"</code>, <code>\"toggleOne\"</code>, <code>\"getCollections\"</code>, <code>\"ensureCollection\"</code>, <code>\"issetCollection\"</code>, <code>\"removeCollection\"</code> - Wildcard event: <code>\"*\"</code> (fires for all operations)</p> <p>Event Handler Signature: <pre><code>(query: VQuery, result: any) =&gt; void\n// or for wildcard:\n(op: string, query: VQuery, result: any) =&gt; void\n</code></pre></p> <p>Example - Subscribe to specific operation:</p> <pre><code>db.emitter.on(\"find\", (query, result) =&gt; {\n  console.log(`Found ${result.length} items in ${query.collection}`);\n});\n\ndb.emitter.on(\"add\", (query, result) =&gt; {\n  console.log(`Added to ${query.collection}:`, result._id);\n});\n</code></pre> <p>Example - Subscribe to all operations:</p> <pre><code>db.emitter.on(\"*\", (op, query, result) =&gt; {\n  metrics.record(op, query.collection, Date.now());\n});\n</code></pre> <p>Example - Audit logging:</p> <pre><code>db.emitter.on(\"*\", (op, query, result) =&gt; {\n  if (op === \"remove\" || op === \"removeOne\") {\n    auditLog.record({\n      action: \"delete\",\n      collection: query.collection,\n      timestamp: new Date()\n    });\n  }\n});\n</code></pre> <p>Note: <code>db.emiter</code> is a deprecated alias for <code>db.emitter</code> (typo preserved for backward compatibility).</p>"},{"location":"api/valthera/#executor","title":"Executor","text":""},{"location":"api/valthera/#executor_1","title":"<code>executor</code>","text":"<p>Controls how database operations are queued and executed. Ensures operations on the same collection are serialized.</p> <p>Type: <code>ExecutorInterface</code></p> <p>Default: <code>SmartExecutor</code> (per-collection queuing with TTL)</p>"},{"location":"api/valthera/#executor-classes","title":"Executor Classes","text":""},{"location":"api/valthera/#executor_2","title":"<code>Executor</code>","text":"<p>Simple FIFO queue. All operations are executed sequentially.</p> <pre><code>import { Executor } from \"@wxn0brp/db-core\";\n\nconst db = new ValtheraClass({\n  adapter: myAdapter,\n  executor: new Executor()\n});\n</code></pre>"},{"location":"api/valthera/#smartexecutor","title":"<code>SmartExecutor</code>","text":"<p>Per-collection queues with automatic cleanup. Operations on different collections can run in parallel.</p> <pre><code>import { SmartExecutor } from \"@wxn0brp/db-core\";\n\nconst db = new ValtheraClass({\n  adapter: myAdapter,\n  executor: new SmartExecutor(\n    ttl,  // default: 300000 (5 min)\n    aware // default: false\n  )\n});\n</code></pre> <p>Parameters: - <code>ttl</code> (default: <code>300000</code>): Time-to-live for inactive queues in milliseconds - <code>aware</code> (default: <code>false</code>): Whether to isolate queues per collection</p> <p>Behavior: - When <code>aware = true</code>: creates separate queues for each collection, operations on different collections run in parallel - When <code>aware = false</code>: all operations share a single queue (behaves like simple <code>Executor</code> but with TTL cleanup) - Idle queues are automatically removed after <code>ttl</code></p> <p>Example - Custom TTL:</p> <pre><code>const db = new ValtheraClass({\n  adapter: myAdapter,\n  executor: new SmartExecutor(10 * 60 * 1000, true) // 10 min, per-collection\n});\n</code></pre> <p>Note: When an adapter has <code>smartExecutor = true</code>, <code>ValtheraClass</code> automatically enables <code>aware</code> mode on the <code>SmartExecutor</code>.</p> <p>See Executor Tutorial for detailed explanation.</p>"},{"location":"dev/adapter/","title":"ValtheraDB Storage Adapter Tutorial","text":"<p>This guide shows you how to create custom storage adapters for ValtheraDB. Adapters are organized into levels based on complexity - start from the level that matches your needs.</p>"},{"location":"dev/adapter/#architecture-overview","title":"Architecture Overview","text":"<p>ValtheraDB uses a layered adapter architecture:</p> <pre><code>|-------------------------------------|\n|        ValtheraClass (API)          |\n|   add, find, update, remove...      |\n|-------------------------------------|\n                  |\n|-------------------------------------|\n|   ActionsBase / CustomActionsBase   |\n|        Collection management        |\n|-------------------------------------|\n                  |\n|-------------------------------------|\n|              FileCpu                |\n|     Low-level file operations       |\n|-------------------------------------|\n                  |\n|-------------------------------------|\n|     Your Storage Implementation     |\n|   (CSV, SQLite, XML, JSON, etc.)    |\n|-------------------------------------|\n</code></pre> <p>Note: When you replace any layer, you implicitly take responsibility for all layers below it</p>"},{"location":"dev/adapter/#level-1-customfilecpu-adapter-easiest","title":"Level 1: CustomFileCpu Adapter (Easiest)","text":"<p>Best for: Simple file-based storage (JSON, CSV, line-delimited formats)</p> <p>What you get: All CRUD operations support.</p> <p>What you implement: Just <code>read</code> and <code>write</code> functions and collection management.</p>"},{"location":"dev/adapter/#example-csv-adapter","title":"Example: CSV Adapter","text":"<p>See full implementation:</p> <p>ValtheraDB-storage-csv/src/action.ts</p> <p>Key points:</p> <ul> <li>Extend <code>CustomActionsBase</code></li> <li>Initialize <code>this.fileCpu = new CustomFileCpu(this.read.bind(this), this.write.bind(this))</code></li> <li>Implement <code>read()</code> - parse file to array of objects</li> <li>Implement <code>write()</code> - write array of objects to file</li> <li>Implement collection management methods (<code>ensureCollection</code>, <code>issetCollection</code>, etc.)</li> </ul>"},{"location":"dev/adapter/#when-to-use-level-1","title":"When to Use Level 1","text":"<p>\u2705 Your data fits in memory (entire file is loaded on each operation) \u2705 Simple file formats (JSON, CSV, YAML) \u2705 You want minimal code \u2705 Prototyping or small-scale applications  </p> <p>\u274c Large datasets (GB+) \u274c Need for partial file reads \u274c Complex query optimization  </p>"},{"location":"dev/adapter/#level-2-actionsbase-with-filecpu-intermediate","title":"Level 2: ActionsBase with FileCpu (Intermediate)","text":"<p>Best for: File-based storage with custom file formats, streaming, or large datasets.</p> <p>What you get: Query utilities. <code>updateOneOrAdd</code> and <code>toggleOne</code> support.</p> <p>What you implement: Full <code>FileCpu</code> interface + file management logic and collection management.</p>"},{"location":"dev/adapter/#interface","title":"Interface","text":"<ul> <li>ValtheraDB-core/src/types/fileCpu.ts</li> </ul>"},{"location":"dev/adapter/#example-line-delimited-json-adapter-dir","title":"Example: Line-Delimited JSON Adapter (dir)","text":"<p>See full implementation:</p> <ul> <li>ValtheraDB-storage-dir/src/file/index.ts - FileCpu implementation</li> <li>ValtheraDB-storage-dir/src/action.ts - ActionsBase implementation</li> </ul> <p>Key points:</p> <ul> <li>Implement <code>FileCpu</code> interface with streaming support</li> <li>Extend <code>ActionsBase</code> for collection management</li> <li>Use <code>findUtil()</code> for multi-file queries</li> <li>Handle file rotation (max file size)</li> </ul>"},{"location":"dev/adapter/#when-to-use-level-2","title":"When to Use Level 2","text":"<p>\u2705 Large files that don't fit in memory \u2705 Custom file formats \u2705 Streaming reads required \u2705 Need control over file organization (sharding, rotation)  </p> <p>\u274c Want minimal implementation code  </p>"},{"location":"dev/adapter/#level-3-full-actionsbase-advanced","title":"Level 3: Full ActionsBase (Advanced)","text":"<p>Best for: Database systems, remote APIs, complex storage or full control.</p> <p>What you get: Some utilities.</p> <p>What you implement: Everything - full control over storage logic.</p>"},{"location":"dev/adapter/#interface_1","title":"Interface","text":"<ul> <li>ValtheraDB-core/src/base/actions.ts</li> </ul>"},{"location":"dev/adapter/#example-sqlite-adapter","title":"Example: SQLite Adapter","text":"<p>See full implementation: ValtheraDB-storage-sqlite/src/index.ts</p> <p>Key points:</p> <ul> <li>Extend <code>ActionsBase</code></li> <li>Translate Valthera queries to SQL</li> <li>Handle prepared statements</li> <li>Map SQL results to Valthera format</li> </ul>"},{"location":"dev/adapter/#example-mongodb-adapter","title":"Example: MongoDB Adapter","text":"<p>See full implementation: ValtheraDB-storage-mongodb/src/actions.ts</p> <p>Key points:</p> <ul> <li>Extend <code>ActionsBase</code></li> <li>Translate Valthera queries to MongoDB format</li> <li>Manage connection lifecycle (<code>init()</code>)</li> </ul>"},{"location":"dev/adapter/#when-to-use-level-3","title":"When to Use Level 3","text":"<p>\u2705 Maximum performance control \u2705 External database systems (SQL, NoSQL) \u2705 Remote APIs / microservices \u2705 Transaction support needed  </p> <p>\u274c Simple file-based storage \u274c Quick prototyping  </p>"},{"location":"dev/adapter/#level-comparison","title":"Level Comparison","text":"Feature Level 1 Level 2 Level 3 Base Class <code>CustomActionsBase</code> <code>ActionsBase</code> + <code>FileCpu</code> <code>ActionsBase</code> Implement <code>read</code>, <code>write</code> <code>FileCpu</code> interface All CRUD methods Memory Usage Full file load Streaming Depends on impl Best For JSON, CSV, XML Custom formats High performance, SQL, NoSQL, APIs Code Lines ~50-80 ~150-250 300+"},{"location":"dev/adapter/#utility-functions-reference","title":"Utility Functions Reference","text":""},{"location":"dev/adapter/#from-wxn0brpdb-core","title":"From <code>@wxn0brp/db-core</code>","text":"<pre><code>// ID generation\nimport { genId } from \"@wxn0brp/db-core\";\nimport { addId } from \"@wxn0brp/db-core/helpers/addId\";\n\n// Query helpers\nimport { hasFieldsAdvanced } from \"@wxn0brp/db-core/utils/hasFieldsAdvanced\";\nimport { updateFindObject } from \"@wxn0brp/db-core/utils/updateFindObject\";\nimport { updateObjectAdvanced } from \"@wxn0brp/db-core/utils/updateObject\";\n\n// Find utility for multi-file searches\nimport { findUtil } from \"@wxn0brp/db-core/utils/action\";\n\n// Types\nimport type { FileCpu } from \"@wxn0brp/db-core/types/fileCpu\";\nimport type { Data, DataInternal } from \"@wxn0brp/db-core/types/data\";\nimport type { VQueryT } from \"@wxn0brp/db-core/types/query\";\nimport type { AdapterOpts } from \"@wxn0brp/db-core/types/options\";\n</code></pre>"},{"location":"dev/adapter/#next-steps","title":"Next Steps","text":"<ul> <li>Read Core Concepts for ValtheraDB philosophy</li> <li>Explore Updater Operators for advanced updates</li> <li>Learn about Relations for cross-collection queries</li> <li>Browse the Ecosystem for ready-made storage adapters</li> </ul>"},{"location":"dev/executor/","title":"Understanding the Executor","text":"<p>The Executor is a core component that ensures database operations are executed safely and in the correct order. It prevents race conditions by serializing operations on the same collection.</p>"},{"location":"dev/executor/#why-you-need-an-executor","title":"Why You Need an Executor","text":"<p>Database adapters often cannot handle concurrent operations safely. Without an executor:</p> <pre><code>// Race condition without executor\nawait Promise.all([\n  db.users.add({ name: \"Alice\" }),\n  db.users.add({ name: \"Bob\" })\n]);\n// Both operations might interfere with each other\n</code></pre> <p>With an executor, operations are queued and executed sequentially:</p> <pre><code>// Safe with executor\nawait Promise.all([\n  db.users.add({ name: \"Alice\" }),\n  db.users.add({ name: \"Bob\" })\n]);\n// Operations are queued: Alice first, then Bob\n</code></pre>"},{"location":"dev/executor/#executor-types","title":"Executor Types","text":""},{"location":"dev/executor/#executor-simple","title":"Executor (Simple)","text":"<p>A basic FIFO queue. All operations are executed one after another, regardless of collection.</p> <pre><code>import { Executor, ValtheraClass } from \"@wxn0brp/db-core\";\n\nconst db = new ValtheraClass({\n  adapter: myAdapter,\n  executor: new Executor()\n});\n</code></pre> <p>Characteristics:</p> <ul> <li>Single queue for all operations</li> <li>Strict sequential execution</li> <li>Simple and predictable</li> <li>Lower concurrency (bottleneck on single queue)</li> </ul> <p>When to use:</p> <ul> <li>Simple adapters that cannot handle any concurrency</li> <li>When you want predictable, linear execution</li> <li>Testing and debugging</li> </ul>"},{"location":"dev/executor/#smartexecutor-advanced","title":"SmartExecutor (Advanced)","text":"<p>Per-collection queues with automatic cleanup. Operations on different collections can run in parallel.</p> <pre><code>import { SmartExecutor, ValtheraClass } from \"@wxn0brp/db-core\";\n\nconst db = new ValtheraClass({\n  adapter: myAdapter,\n  executor: new SmartExecutor(\n    ttl,  // default: 300000 (5 min)\n    aware // default: false\n  )\n});\n</code></pre> <p>Characteristics:</p> <ul> <li>Separate queue for each collection</li> <li>Parallel execution across collections</li> <li>Automatic cleanup of idle queues</li> <li>Higher concurrency</li> </ul> <p>Parameters:</p> <ul> <li><code>ttl</code> (default: <code>300000</code>): Time-to-live for inactive queues (milliseconds)</li> <li><code>aware</code> (default: <code>false</code>): Whether to isolate queues per collection</li> </ul> <p>When to use:</p> <ul> <li>Most production scenarios</li> <li>Applications with multiple collections</li> <li>When you need better concurrency</li> </ul>"},{"location":"dev/executor/#how-smartexecutor-works","title":"How SmartExecutor Works","text":""},{"location":"dev/executor/#queue-isolation","title":"Queue Isolation","text":"<pre><code>// These operations run in parallel (different collections)\nawait Promise.all([\n  db.users.add({ name: \"Alice\" }),    // Queue: \"users\"\n  db.posts.add({ title: \"Hello\" }),   // Queue: \"posts\"\n  db.comments.add({ text: \"Nice!\" })  // Queue: \"comments\"\n]);\n\n// These operations run sequentially (same collection)\nawait Promise.all([\n  db.users.add({ name: \"Alice\" }),    // Queue: \"users\" - first\n  db.users.add({ name: \"Bob\" }),      // Queue: \"users\" - second\n  db.users.find({})                    // Queue: \"users\" - third\n]);\n</code></pre>"},{"location":"dev/executor/#automatic-cleanup","title":"Automatic Cleanup","text":"<p>SmartExecutor automatically removes idle queues after the TTL period:</p> <pre><code>const executor = new SmartExecutor(60000); // 1 minute TTL\n\n// Operation creates a queue for \"users\"\nawait db.users.add({ name: \"Alice\" });\n\n// Queue remains active for 1 minute\n// If no operations on \"users\" for 1 minute, queue is removed\n// Next operation on \"users\" creates a new queue\n</code></pre>"},{"location":"dev/executor/#aware-mode","title":"aware Mode","text":"<p>When <code>aware = true</code>, queues are isolated per collection:</p> <pre><code>const executor = new SmartExecutor(300000, true);\n// \"users\" -&gt; separate queue\n// \"posts\" -&gt; separate queue\n// \"comments\" -&gt; separate queue\n</code></pre> <p>When <code>aware = false</code> (default), all operations share a single queue:</p> <pre><code>const executor = new SmartExecutor(300000, false);\n// All operations -&gt; single shared queue\n// Behaves like simple Executor but with TTL cleanup\n</code></pre>"},{"location":"dev/executor/#adapter-integration","title":"Adapter Integration","text":"<p>Adapters can signal support for SmartExecutor:</p> <pre><code>class MyAdapter extends ActionsBase {\n  smartExecutor = true; // Signals support for per-collection queuing\n\n  // ... adapter methods\n}\n</code></pre> <p>When an adapter has <code>smartExecutor = true</code>, ValtheraClass automatically enables <code>aware</code> mode on the SmartExecutor.</p>"},{"location":"dev/executor/#custom-executor","title":"Custom Executor","text":"<p>You can implement your own executor:</p> <pre><code>import { ExecutorInterface } from \"@wxn0brp/db-core\";\n\nclass MyCustomExecutor implements ExecutorInterface {\n  async addOp(func: Function, query?: any, collection?: string): Promise&lt;any&gt; {\n    // Your custom logic here\n    // Must return a promise that resolves with the operation result\n\n    return await new Promise((resolve, reject) =&gt; {\n      // Queue the operation\n      // Call func(query) when ready\n      func(query).then(resolve).catch(reject);\n    });\n  }\n}\n\nconst db = new ValtheraClass({\n  adapter: myAdapter,\n  executor: new MyCustomExecutor()\n});\n</code></pre>"},{"location":"dev/executor/#performance-considerations","title":"Performance Considerations","text":""},{"location":"dev/executor/#executor-vs-smartexecutor","title":"Executor vs SmartExecutor","text":"Scenario Executor SmartExecutor Single collection Same Same Multiple collections Sequential Parallel Memory usage Low Higher (multiple queues) Concurrency Low High Complexity Simple Moderate"},{"location":"dev/plugin/","title":"Writing Plugins","text":"<p>Plugins let you intercept and modify database operations before they reach the storage adapter. They're perfect for logging, caching, validation, or adding custom behavior across all operations.</p>"},{"location":"dev/plugin/#plugin-interface","title":"Plugin Interface","text":"<p>A plugin is an object with three properties:</p> <pre><code>interface ValtheraPlugin {\n  name: string;\n  init?: (db: ValtheraClass) =&gt; void;\n  execute(ctx: PluginContext): Promise&lt;any&gt;;\n}\n</code></pre> <ul> <li><code>name</code>: Unique identifier for the plugin</li> <li><code>init</code> (optional): Called once when the plugin is registered</li> <li><code>execute</code>: Called for every database operation</li> </ul>"},{"location":"dev/plugin/#context-object","title":"Context Object","text":"<p>The <code>execute</code> method receives a context object:</p> <pre><code>interface PluginContext {\n  op: string;        // Operation name: \"find\", \"add\", \"update\", etc.\n  query: any;        // The query object (or collection name string for some ops)\n  next: () =&gt; Promise&lt;any&gt;; // Call the next plugin or adapter\n}\n</code></pre> <p>The <code>op</code> field can be any of:</p> <ul> <li><code>getCollections</code>, <code>ensureCollection</code>, <code>issetCollection</code>, <code>removeCollection</code></li> <li><code>add</code>, <code>find</code>, <code>findOne</code>, <code>update</code>, <code>updateOne</code>, <code>remove</code>, <code>removeOne</code></li> <li><code>updateOneOrAdd</code>, <code>toggleOne</code></li> </ul>"},{"location":"dev/plugin/#basic-plugin-structure","title":"Basic Plugin Structure","text":"<p>Here's the minimal plugin structure:</p> <pre><code>import { ValtheraPlugin } from \"@wxn0brp/db\";\n\nconst myPlugin: ValtheraPlugin = {\n  name: \"my-plugin\",\n\n  async execute(ctx) {\n    // Before the operation\n    console.log(`About to execute: ${ctx.op}`);\n\n    // Call next() to continue the chain\n    const result = await ctx.next();\n\n    // After the operation\n    console.log(`Finished: ${ctx.op}`);\n\n    return result;\n  }\n};\n</code></pre>"},{"location":"dev/plugin/#registering-plugins","title":"Registering Plugins","text":"<p>Use the <code>plugin()</code> method to register:</p> <pre><code>const db = ValtheraCreate(\"./my-db\");\n\nconst unregister = db.plugin(myPlugin);\n\n// Later, to remove the plugin:\nunregister();\n</code></pre> <p>The <code>plugin()</code> method returns a function that removes the plugin when called.</p>"},{"location":"dev/plugin/#execution-order","title":"Execution Order","text":"<p>Plugins form a chain. When you call a database operation:</p> <ol> <li>The first plugin's <code>execute</code> is called</li> <li>It can call <code>ctx.next()</code> to invoke the second plugin</li> <li>The second plugin calls <code>ctx.next()</code> to invoke the third</li> <li>...and so on until the adapter is reached</li> <li>Results flow back through the chain</li> </ol> <p>Example with multiple plugins:</p> <pre><code>db.plugin(pluginA);\ndb.plugin(pluginB);\ndb.plugin(pluginC);\n\nawait db.collection(\"users\").find({});\n// Execution order: A -&gt; B -&gt; C -&gt; adapter -&gt; C -&gt; B -&gt; A\n</code></pre>"},{"location":"dev/plugin/#practical-examples","title":"Practical Examples","text":""},{"location":"dev/plugin/#logging-plugin","title":"Logging Plugin","text":"<p>Log all operations with timing:</p> <pre><code>const loggingPlugin: ValtheraPlugin = {\n  name: \"logger\",\n\n  async execute(ctx) {\n    const start = Date.now();\n    console.log(`[START] ${ctx.op}`, ctx.query);\n\n    const result = await ctx.next();\n\n    const duration = Date.now() - start;\n    console.log(`[END] ${ctx.op} (${duration}ms)`);\n\n    return result;\n  }\n};\n</code></pre>"},{"location":"dev/plugin/#operation-blocker","title":"Operation Blocker","text":"<p>Prevent certain operations:</p> <pre><code>const readOnlyPlugin: ValtheraPlugin = {\n  name: \"read-only\",\n\n  async execute(ctx) {\n    const writeOps = [\"add\", \"update\", \"updateOne\", \"remove\", \"removeOne\"];\n\n    if (writeOps.includes(ctx.op)) {\n      throw new Error(`Operation ${ctx.op} is not allowed in read-only mode`);\n    }\n\n    return ctx.next();\n  }\n};\n</code></pre>"},{"location":"dev/plugin/#modifying-queries","title":"Modifying Queries","text":"<p>Plugins can modify queries before they reach the adapter:</p> <pre><code>const softDeletePlugin: ValtheraPlugin = {\n  name: \"soft-delete\",\n\n  async execute(ctx) {\n    // Add deleted filter to all find operations\n    if (ctx.op === \"find\" || ctx.op === \"findOne\") {\n      ctx.query.search ||= {};\n      ctx.query.search.deleted = { $ne: true };\n    }\n\n    return ctx.next();\n  }\n};\n</code></pre>"},{"location":"dev/plugin/#modifying-results","title":"Modifying Results","text":"<p>Transform results after the adapter returns them:</p> <pre><code>const transformPlugin: ValtheraPlugin = {\n  name: \"transformer\",\n\n  async execute(ctx) {\n    const result = await ctx.next();\n\n    if (ctx.op === \"find\" &amp;&amp; Array.isArray(result)) {\n      return result.map(item =&gt; ({\n        ...item,\n        fullName: `${item.firstName} ${item.lastName}`\n      }));\n    }\n\n    return result;\n  }\n};\n</code></pre>"},{"location":"ecosystem/adapters/","title":"Storage Adapters","text":"<p>ValtheraDB uses pluggable storage engines - every adapter implements the same <code>ActionsBase</code> interface with identical CRUD API.</p>"},{"location":"ecosystem/adapters/#quick-start-vdb-api","title":"Quick Start: VDB() API","text":"<p>The easiest way to use adapters - auto-resolves based on config or env vars.</p> <pre><code>bun add @wxn0brp/db-resolver @wxn0brp/db-core\n</code></pre> <pre><code>import { VDB } from \"@wxn0brp/db-resolver\";\n\n// Dir adapter (default)\nconst db = VDB().dir(\"./data\");\n\n// SQLite with Bun\nconst db = VDB().sqlite.bun(\"db.sqlite\");\n\n// MongoDB\nconst db = VDB().mongodb(\"mongodb://localhost:27017\", \"mydb\");\n\n// Typed collections\nconst db = VDB&lt;{ users: { id: number; name: string } }&gt;().dir(\"./data\");\nawait db.users.add({ id: 1, name: \"Ala\" });\n</code></pre> <p>Resolution order: <code>force</code> option -&gt; <code>VALTHERA_&lt;NAME&gt;</code> env var -&gt; <code>def</code> fallback -&gt; <code>./vdb-data/&lt;name&gt;</code></p> <p>In development, missing adapters are auto-installed.</p> <p>-&gt; Full Resolver Docs</p>"},{"location":"ecosystem/adapters/#manual-installation","title":"Manual Installation","text":"<p>To use an adapter manually, install the package and create an instance.</p> Adapter Storage Install Repo dir Directory of JSON5/JSON/YAML files <code>bun add @wxn0brp/db-storage-dir</code> GitHub sqlite SQLite (Bun/Node/better-sqlite3) <code>bun add @wxn0brp/db-storage-sqlite</code> GitHub mongodb MongoDB <code>bun add @wxn0brp/db-storage-mongodb</code> GitHub bin Single binary file (msgpack) <code>bun add @wxn0brp/db-storage-bin</code> GitHub c Native C via bun:ffi (Linux) built with <code>./build.sh</code> GitHub csv CSV files <code>bun add @wxn0brp/db-storage-csv</code> GitHub web-storage Browser localStorage/sessionStorage/IndexedDB <code>bun add @wxn0brp/db-storage-web</code> GitHub length Fixed-length binary records <code>bun add @wxn0brp/db-storage-length</code> GitHub crypt AES-256-GCM encrypted (wraps adapter) <code>bun add @wxn0brp/db-storage-crypt</code> GitHub rocks RocksDB key-value <code>bun add @wxn0brp/db-storage-rocks</code> GitHub accdb MS Access via ODBC <code>bun add @wxn0brp/db-storage-accdb</code> GitHub"},{"location":"ecosystem/adapters/#creating-a-custom-adapter","title":"Creating a Custom Adapter","text":"<p>See the Adapter Tutorial for a step-by-step guide on building your own storage adapter.</p>"},{"location":"ecosystem/cli/","title":"CLI","text":"<p>The <code>vdb</code> command-line tool for managing ValtheraDB databases. Supports add, find, update, remove, dev server, format, and upgrade.</p> <pre><code>bun add -g @wxn0brp/db-cli\nvdb -d ./my-db find users '{ \"name\": \"Jane\" }'\nvdb server 3000\n</code></pre> <p>-&gt; GitHub</p>"},{"location":"ecosystem/conduit/","title":"Conduit","text":"<p>Embeds ValtheraDB as a compiled Bun binary, communicates over stdin/stdout with a framed binary protocol (12-byte header + JSON payload).</p> <p>Lets you drive ValtheraDB from any language - Go, Python, Rust, etc.</p>"},{"location":"ecosystem/conduit/#python-bridge","title":"Python Bridge","text":"<pre><code>pip install wxn0brp-db-conduit\n</code></pre> <pre><code>from wxn0brp.db import conduit\n\nwith conduit.ValtheraConduit(\"./vendor/vdb\", auto_download=True) as c:\n    db = c.data.init(\"./data\")\n    users = db.c(\"users\")\n    users.add({\"name\": \"Ada\"})\n    print(users.find({\"name\": \"Ada\"}))\n</code></pre>"},{"location":"ecosystem/conduit/#protocol-summary","title":"Protocol Summary","text":"<ul> <li>12-byte LE header: frameType (u32) + dbNameLength (u32) + payloadLength (u32)</li> <li>Frame types: INIT_DB, EXECUTE_JSON, CLOSE_DB, LIST_DBS, PING, SHUTDOWN</li> <li>Max payload: 64MB, Max DB name: 4096 bytes</li> </ul> <p>-&gt; Full Protocol Docs |  Python Bridge</p>"},{"location":"ecosystem/extensions/","title":"Extensions","text":"<p>Extensions add functionality on top of ValtheraDB - replication, indexing, and concurrency control.</p> Package What it does Install Repo <code>@wxn0brp/db-crdt</code> CRDT replication and sync between ValtheraDB instances <code>bun add @wxn0brp/db-crdt</code> GitHub <code>@wxn0brp/db-lock</code> File-based multi-process locking with stale detection <code>bun add @wxn0brp/db-lock</code> GitHub"},{"location":"ecosystem/server/","title":"Server","text":"<p>HTTP server for ValtheraDB on port 14785. Built on FalconFrame with JWT auth, GateWarden permissions (RBAC/ACL/ABAC), Web GUI, and SQL/CSV import/export.</p>"},{"location":"ecosystem/server/#quick-start","title":"Quick Start","text":"<pre><code>docker run -p 14785:14785 ghcr.io/wxn0brp/valthera-server\n</code></pre>"},{"location":"ecosystem/server/#php-server-mariadbmysql","title":"PHP Server (MariaDB/MySQL)","text":"<p>Alternative PHP implementation for SQL databases.</p> <pre><code>git clone https://github.com/wxn0brP/ValtheraDB-server-php.git\n</code></pre> <p>Configure via <code>config.php</code>, API compatible with main server.</p> <p>-&gt; Full Server Docs | PHP Server</p>"},{"location":"ecosystem/tools/","title":"Other Tools","text":"Tool What it does Repo <code>@wxn0brp/db</code> Meta-package - bundles core + dir adapter + client for zero-config use GitHub <code>@wxn0brp/db-core</code> Core engine - provides ValtheraClass, Collection, Relation, operators, types GitHub <code>@wxn0brp/db-resolver</code> Auto-selects storage adapter via config, env var (<code>VALTHERA_&lt;NAME&gt;</code>), or <code>force</code> option GitHub <code>ValtheraDB-e2e</code> Compliance test suite - runs predefined tests against any ActionsBase adapter GitHub <code>ValtheraDB-benchmark</code> Cross-adapter CRUD benchmarks with web visualization GitHub <code>ValtheraDB-squirrel</code> AP routing layer - hash-based distribution over unreliable storage nodes GitHub <code>@wxn0brp/db-client</code> Remote client (<code>ValtheraRemote</code>) - proxies operations via HTTP to a ValtheraDB server GitHub <code>@wxn0brp/db-string-query</code> Parses queries in SQL-like (<code>SELECT *</code>) syntax GitHub"}]}